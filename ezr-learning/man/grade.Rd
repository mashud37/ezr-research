% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quiz.R
\name{grade}
\alias{grade}
\title{Grade a whole quiz at once}
\usage{
grade(quiz, answers)
}
\arguments{
\item{quiz}{An \code{ezrlearning_quiz} from \code{\link[=quiz]{quiz()}}.}

\item{answers}{A list of answers, one per question, in order (or named by
exercise id). Each element is whatever \code{\link[=check_answer]{check_answer()}} accepts (a letter for
MCQ, a value / code string for code tasks). Use \code{NULL} to skip a question.}
}
\value{
An \code{ezrlearning_grade} object: a per-item table and the \code{score}
(correct out of those that could be auto-graded). Has a \code{print()} method.
}
\description{
Checks a list of answers against a quiz and reports a per-question result and
an overall score.
}
\details{
Answers are matched to questions by name (exercise id) when the list is named,
otherwise by position. Self-graded code tasks (those without a stored answer)
are reported as \code{NA} and left out of the score denominator. The returned table
makes it easy to see which topics need another pass.
}
\examples{
q <- quiz(3, type = "mcq", seed = 1)
# answer every question "A" (some right, some wrong)
grade(q, as.list(rep("A", 3)))
}
\seealso{
\code{\link[=quiz]{quiz()}}, \code{\link[=check_answer]{check_answer()}}.

Other quiz:
\code{\link[=export_worksheet]{export_worksheet()}},
\code{\link[=quiz]{quiz()}}
}
\concept{quiz}
