% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bank.R
\name{draw_exercise}
\alias{draw_exercise}
\title{Draw a practice exercise}
\usage{
draw_exercise(
  topic = NULL,
  chapter = NULL,
  type = NULL,
  difficulty = NULL,
  seed = NULL
)
}
\arguments{
\item{topic}{Topic(s) to draw from (see \code{\link[=list_topics]{list_topics()}}). \code{NULL} = any.}

\item{chapter}{Book chapter number(s) to draw from. \code{NULL} = any.}

\item{type}{\code{"mcq"}, \code{"code"}, or \code{NULL} for either.}

\item{difficulty}{\code{"easy"}, \code{"medium"}, \code{"hard"}, or \code{NULL} for any. Defaults
to the \code{difficulty} option only as a tie-break when set explicitly; \code{NULL}
here means "do not filter".}

\item{seed}{Optional integer. If \code{NULL}, a random seed is chosen (and stored
on the exercise as \verb{$seed} so you can reproduce it).}
}
\value{
An \code{ezrlearning_exercise} (with \code{print()}; answer it with
\code{\link[=check_answer]{check_answer()}}, get a \code{\link[=hint]{hint()}} or \code{\link[=reveal]{reveal()}} it).
}
\description{
Picks one built-in exercise (optionally filtered by topic, chapter, type or
difficulty) and instantiates a randomised version of it. The draw is
reproducible: the same \code{seed} always yields the same exercise, different seeds
yield different ones.
}
\details{
Generators are templates: each draw sub-samples the teaching data and varies
the target column or option order, so a learner can keep drilling the same
topic without memorising one fixed answer. Because the whole draw (which
template, and the instance) is seeded, a teacher can hand out
\code{draw_exercise(topic = "nps", seed = 42)} and everyone gets the identical
question. The exercise records its \code{seed}.
}
\examples{
x <- draw_exercise(topic = "nps", seed = 1)
x
check_answer(x, "A")
}
\seealso{
\code{\link[=quiz]{quiz()}}, \code{\link[=check_answer]{check_answer()}}, \code{\link[=exercise_bank]{exercise_bank()}}.

Other bank:
\code{\link[=exercise_bank]{exercise_bank()}},
\code{\link[=list_topics]{list_topics()}}
}
\concept{bank}
