% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{ezrlearning_options}
\alias{ezrlearning_options}
\title{Get or set ezrlearning defaults}
\usage{
ezrlearning_options(...)
}
\arguments{
\item{...}{Either nothing (to read all values) or named \code{option = value}
pairs to set.}
}
\value{
When reading, a named list of all current values. When setting, the
previous values, invisibly.
}
\description{
ezrlearning reads a few defaults from R options (prefixed \code{ezrlearning.}) so
you can tune behaviour once instead of repeating arguments. Call with no
arguments to see the current effective values; call with \code{name = value} pairs
to set them for the session.
}
\details{
Recognised options:
\describe{
\item{\code{difficulty}}{Default difficulty for \code{\link[=draw_exercise]{draw_exercise()}} / \code{\link[=quiz]{quiz()}}:
\code{"easy"}, \code{"medium"} or \code{"hard"}.}
\item{\code{seed}}{Default seed for draws; \code{NULL} means a fresh random draw each
call.}
\item{\code{provider}}{Default LLM provider for \code{\link[=generate_exercise]{generate_exercise()}}.}
\item{\code{model}}{Default LLM model (\code{NULL} = the provider's default).}
}

Options let you set a house style once -- e.g. a default \code{difficulty} for a
cohort, or a \code{provider} for the AI helpers -- without threading the argument
through every call. Setting returns the previous values invisibly, so you can
restore them later. Use \code{\link[=reset_ezrlearning_options]{reset_ezrlearning_options()}} to clear everything back
to the built-in defaults.
}
\examples{
ezrlearning_options()                 # view all defaults
ezrlearning_options(difficulty = "medium")
ezrlearning_options()$difficulty      # "medium"
reset_ezrlearning_options()
}
\seealso{
\code{\link[=reset_ezrlearning_options]{reset_ezrlearning_options()}}.

Other config:
\code{\link[=reset_ezrlearning_options]{reset_ezrlearning_options()}}
}
\concept{config}
