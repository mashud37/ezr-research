% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bank.R
\name{exercise_bank}
\alias{exercise_bank}
\title{The catalogue of built-in exercises}
\usage{
exercise_bank()
}
\value{
A \link[tibble:tibble]{tibble} with one row per generator: \code{id},
\code{chapter}, \code{topic}, \code{type} (\code{"mcq"}/\code{"code"}), \code{difficulty} and a short
\code{title}.
}
\description{
Lists every built-in exercise generator with its chapter, topic, type and
difficulty -- the menu \code{\link[=draw_exercise]{draw_exercise()}} and \code{\link[=quiz]{quiz()}} draw from.
}
\details{
Each row is a \emph{template}, not a fixed question: drawing it with a different
\code{seed} produces a different concrete instance (different sample, target column
or shuffled options). Filter on \code{topic}/\code{chapter}/\code{type}/\code{difficulty} to scope
a practice session, then pass the same filters to \code{\link[=draw_exercise]{draw_exercise()}} or
\code{\link[=quiz]{quiz()}}.
}
\examples{
exercise_bank()
}
\seealso{
\code{\link[=draw_exercise]{draw_exercise()}}, \code{\link[=quiz]{quiz()}}, \code{\link[=list_topics]{list_topics()}}.

Other bank:
\code{\link[=draw_exercise]{draw_exercise()}},
\code{\link[=list_topics]{list_topics()}}
}
\concept{bank}
