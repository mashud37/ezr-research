% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quiz.R
\name{quiz}
\alias{quiz}
\title{Assemble a quiz of practice exercises}
\usage{
quiz(
  n = 5,
  topic = NULL,
  chapter = NULL,
  type = NULL,
  difficulty = NULL,
  seed = NULL
)
}
\arguments{
\item{n}{Number of exercises. Default \code{5}.}

\item{topic, chapter, type, difficulty}{Filters passed through to the bank (see
\code{\link[=draw_exercise]{draw_exercise()}}). \code{NULL} means no filter.}

\item{seed}{Optional integer seed. If \code{NULL}, one is chosen and stored.}
}
\value{
An \code{ezrlearning_quiz}: a list of \code{exercises} plus the \code{call} used to
build it. Has \code{print()} methods; grade it with \code{\link[=grade]{grade()}} or hand it out with
\code{\link[=export_worksheet]{export_worksheet()}}.
}
\description{
Draws \code{n} exercises (optionally scoped by topic, chapter, type or difficulty)
into a single quiz. Like \code{\link[=draw_exercise]{draw_exercise()}}, a quiz is fully reproducible from
its \code{seed}: the same call always yields the same set of questions, which is
how \code{\link[=export_worksheet]{export_worksheet()}} can rebuild it exactly.
}
\details{
Generators are sampled without replacement when there are at least \code{n} matching
templates (so a short quiz spans different topics), with replacement otherwise.
Each exercise is instantiated with its own sub-seed drawn from the quiz seed,
so the whole quiz is deterministic yet varied. The construction call is stored
on the object, letting \code{\link[=export_worksheet]{export_worksheet()}} reconstruct the identical quiz
inside a worksheet.
}
\examples{
q <- quiz(3, seed = 1)
q
}
\seealso{
\code{\link[=grade]{grade()}}, \code{\link[=export_worksheet]{export_worksheet()}}, \code{\link[=draw_exercise]{draw_exercise()}}.

Other quiz:
\code{\link[=export_worksheet]{export_worksheet()}},
\code{\link[=grade]{grade()}}
}
\concept{quiz}
