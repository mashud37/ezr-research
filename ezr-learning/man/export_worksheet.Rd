% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quiz.R
\name{export_worksheet}
\alias{export_worksheet}
\title{Export a quiz as a self-contained worksheet}
\usage{
export_worksheet(
  quiz,
  path,
  answer_key = FALSE,
  title = "ezr practice worksheet"
)
}
\arguments{
\item{quiz}{An \code{ezrlearning_quiz} from \code{\link[=quiz]{quiz()}}.}

\item{path}{Output path. If it ends in \code{.html} and \code{rmarkdown} is installed,
the worksheet is rendered; otherwise a \code{.Rmd} is written.}

\item{answer_key}{Include the worked solutions (via \code{\link[=reveal]{reveal()}}) after each
question. Default \code{FALSE}.}

\item{title}{Worksheet title. Default \code{"ezr practice worksheet"}.}
}
\value{
Invisibly the path written.
}
\description{
Writes a quiz to an R Markdown worksheet that \emph{rebuilds the identical quiz}
from its seed, so every question -- with its exact data -- is live and
gradable inside the document. Optionally renders it to HTML.
}
\details{
The worksheet's setup chunk calls \code{quiz()} with the same arguments and seed,
so \code{q$exercises[[i]]} is reconstructed exactly -- learners run
\code{check_answer(q$exercises[[i]], ...)} right in the document. Each question
links back to its book chapter. With \code{answer_key = TRUE} the solutions are
included, giving you a teacher's copy. Rendering to HTML needs the suggested
\code{rmarkdown} package; without it (or with a \code{.Rmd} path) the source is written
for you to knit.
}
\examples{
q <- quiz(3, seed = 1)
f <- tempfile(fileext = ".Rmd")
export_worksheet(q, f)
file.exists(f)
}
\seealso{
\code{\link[=quiz]{quiz()}}, \code{\link[=grade]{grade()}}.

Other quiz:
\code{\link[=grade]{grade()}},
\code{\link[=quiz]{quiz()}}
}
\concept{quiz}
