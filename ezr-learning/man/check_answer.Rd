% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exercise.R
\name{check_answer}
\alias{check_answer}
\title{Check an answer to an exercise}
\usage{
check_answer(x, answer, reveal = FALSE)
}
\arguments{
\item{x}{An exercise (from \code{\link[=draw_exercise]{draw_exercise()}}, \code{\link[=quiz]{quiz()}} or
\code{\link[=generate_exercise]{generate_exercise()}}).}

\item{answer}{Your response. MCQ: \code{"B"}, \code{2}, or the option text. Code: a
value, a code string (e.g. \code{"calc_nps(theme_park, nps)"}), or a one-sided
formula (\code{~ calc_nps(theme_park, nps)}).}

\item{reveal}{If \code{TRUE}, include the reference solution in the result when you
are wrong. Default \code{FALSE}.}
}
\value{
An \code{ezrlearning_result} object (with a \code{print()} method) carrying
\code{correct} (logical) and a feedback \code{message}.
}
\description{
Grades a learner's response. For a multiple-choice question pass the option
(a letter, number or the option text). For a code task pass either the value
you computed, or your code as a string or one-sided formula -- it is run
against the exercise data (with the ezr packages available) and compared to
the reference result.
}
\details{
Code answers are evaluated in a sandbox environment where the ezr family's
exported functions and the exercise's \code{data} (under its usual name, e.g.
\code{theme_park}) are in scope, so you do not have to attach anything. The result
is compared to the reference value tolerantly (numeric rounding and row order
do not matter). If your code errors, you are told what went wrong rather than
the call failing. AI-generated \emph{code} tasks have no computed reference value,
so they are self-graded -- \code{check_answer()} will point you to \code{\link[=reveal]{reveal()}}.
}
\examples{
x <- draw_exercise(topic = "percentage", type = "mcq", seed = 1)
check_answer(x, "A")
}
\seealso{
\code{\link[=reveal]{reveal()}}, \code{\link[=hint]{hint()}}, \code{\link[=grade]{grade()}}.

Other exercises:
\code{\link[=hint]{hint()}},
\code{\link[=reveal]{reveal()}}
}
\concept{exercises}
