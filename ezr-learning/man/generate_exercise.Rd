% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ai.R
\name{generate_exercise}
\alias{generate_exercise}
\title{Generate a fresh exercise with a language model}
\usage{
generate_exercise(
  topic,
  type = "mcq",
  difficulty = ezrlearning_default("difficulty"),
  template = NULL,
  provider = ezrlearning_default("provider"),
  model = NULL,
  chat = NULL,
  ...
)
}
\arguments{
\item{topic}{What the exercise should be about (free text, e.g. \code{"NPS by group"} or a \code{\link[=list_topics]{list_topics()}} topic).}

\item{type}{\code{"mcq"} or \code{"code"}. Default \code{"mcq"}.}

\item{difficulty}{\code{"easy"}, \code{"medium"} or \code{"hard"}. Defaults to the
\code{difficulty} option.}

\item{template}{Prompt template (see \code{\link[=list_prompts]{list_prompts()}}). Defaults to the one
matching \code{type}.}

\item{provider, model}{Passed to \code{\link[=ai_chat]{ai_chat()}} when \code{chat} is not supplied.}

\item{chat}{An existing \code{\link[=ai_chat]{ai_chat()}} object to reuse.}

\item{...}{Passed to \code{\link[=ai_chat]{ai_chat()}}.}
}
\value{
An \code{ezrlearning_exercise} with \code{source = "ai"}.
}
\description{
Asks an LLM to write a new exercise about an ezr topic, in the same shape as
the built-in ones, so it slots into \code{\link[=check_answer]{check_answer()}} / \code{\link[=reveal]{reveal()}} and quizzes.
The model is pinned to real ezr functions and the teaching dataset.
}
\details{
Generated \strong{MCQs are auto-graded} (the model supplies the correct option), so
\code{\link[=check_answer]{check_answer()}} works as usual. Generated \strong{code tasks are self-graded}:
there is no precomputed reference value, so \code{check_answer()} will point you to
\code{\link[=reveal]{reveal()}} to compare against the model's solution. The system prompt restricts
the model to the real ezr function inventory and the \code{theme_park} dataset, but
always sanity-check AI output before handing it to learners. Requires the
suggested \code{ellmer} (and \code{jsonlite}) packages and a stored key (see
\code{\link[=set_llm_key]{set_llm_key()}}).
}
\examples{
\dontrun{
set_llm_key("openai")
ex <- generate_exercise("NPS by region", type = "mcq")
ex
}
}
\seealso{
\code{\link[=generate_quiz]{generate_quiz()}}, \code{\link[=draw_exercise]{draw_exercise()}}, \code{\link[=list_prompts]{list_prompts()}}.

Other ai:
\code{\link[=ai_chat]{ai_chat()}},
\code{\link[=delete_llm_key]{delete_llm_key()}},
\code{\link[=generate_quiz]{generate_quiz()}},
\code{\link[=get_llm_key]{get_llm_key()}},
\code{\link[=get_prompt]{get_prompt()}},
\code{\link[=has_llm_key]{has_llm_key()}},
\code{\link[=list_llm_keys]{list_llm_keys()}},
\code{\link[=list_prompts]{list_prompts()}},
\code{\link[=register_prompt]{register_prompt()}},
\code{\link[=set_llm_key]{set_llm_key()}}
}
\concept{ai}
