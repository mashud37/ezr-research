% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{cluster}
\alias{cluster}
\title{Cluster the rows of a dataset, with diagnostics and profiles}
\usage{
cluster(
  data = NULL,
  k = NULL,
  method = c("kmeans", "hclust", "pam"),
  vars = NULL,
  scale = ezrmodel_default("scale"),
  dist = "euclidean"
)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{k}{Number of clusters. If \code{NULL} (default), chosen automatically by
silhouette (needs the \code{cluster} package).}

\item{method}{\code{"kmeans"} (default), \code{"hclust"} or \code{"pam"} (PAM needs the
\code{cluster} package).}

\item{vars}{Columns to cluster on (tidyselect). Default: all numeric columns.}

\item{scale}{Standardise variables first. Default: the \code{scale} option (\code{TRUE}).}

\item{dist}{Distance for \code{hclust}/\code{pam}: \code{"euclidean"} (default),
\code{"manhattan"}, \code{"maximum"}, or \code{"binary"} (Jaccard, for 0/1 data).}
}
\value{
An \code{ezrmodel_clusters} object: \code{assignments}, \code{sizes}, \code{centers},
\code{profile} (per-cluster means), \code{diagnostics} (avg silhouette, within-SS
ratio) and the inputs. Has \code{print()}, \code{plot()}, \code{tidy()} and \code{augment()}.
}
\description{
One-line clustering that returns not just the assignments but the \strong{quality
diagnostics} (silhouette, within-sum-of-squares) and the \strong{cluster profiles}
(what each cluster looks like on every variable) -- everything you need to read
a segmentation.
}
\details{
Variables are standardised by default so that a high-variance column does not
dominate the distances. Rows with missing values on the clustering variables
are dropped. \code{print()} shows the sizes and the average silhouette width (a
rough guide: > 0.5 strong, 0.25-0.5 reasonable, < 0.25 weak). \code{plot()} draws
the clusters on the first two principal components; \code{augment()} appends a
\code{.cluster} column to the kept rows; \code{cluster_profile()} crosses the clusters
against another variable.
}
\examples{
\dontshow{if (requireNamespace("cluster", quietly = TRUE)) withAutoprint(\{ # examplesIf}
cl <- cluster(ecommerce, k = 4,
              vars = c(recency_days, frequency, monetary, tenure_months))
cl

# `personas` is built to cluster cleanly; k is found automatically
cluster(personas, vars = spend_index:browse_minutes)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=cluster_profile]{cluster_profile()}}, \code{\link[=to_matrix]{to_matrix()}}, \code{\link[=reduce_dims]{reduce_dims()}}.

Other clustering:
\code{\link[=cluster_profile]{cluster_profile()}}
}
\concept{clustering}
