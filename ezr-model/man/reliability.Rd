% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/factors.R
\name{reliability}
\alias{reliability}
\title{Scale reliability (Cronbach's alpha)}
\usage{
reliability(data = NULL, items = NULL)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{items}{The item columns (tidyselect). Default: all numeric columns.}
}
\value{
An \code{ezrmodel_reliability} object: \code{alpha} (raw and standardised),
\code{n_items}, \code{n}, a \code{rating} band, and an \code{items} table (item-rest
correlation and alpha-if-dropped). Has \code{print()} and \code{tidy()} methods.
}
\description{
Checks whether a set of items hang together well enough to be averaged into a
single scale, reporting Cronbach's alpha, a plain-language rating, and -- per
item -- the alpha you would get by dropping it (so you can spot a weak item).
}
\details{
Needs the suggested \code{psych} package. Alpha runs 0-1 and rises with both the
number of items and their average inter-correlation; rough bands are
\verb{>= 0.9} excellent, \code{0.8} good, \code{0.7} acceptable, \code{0.6} questionable, below
that poor. The per-item \code{alpha_if_dropped} flags an item whose removal would
\emph{raise} alpha -- usually one that does not belong on the scale. A low item-rest
correlation (\code{r_drop}) is the same warning from a different angle.
}
\examples{
\dontshow{if (requireNamespace("psych", quietly = TRUE)) withAutoprint(\{ # examplesIf}
reliability(nps_drivers, items = c(value, quality, service, ease,
                                   support, trust, price, innovation))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=factors]{factors()}} to find the underlying dimensions.

Other factors:
\code{\link[=factors]{factors()}},
\code{\link[=sem]{sem()}}
}
\concept{factors}
