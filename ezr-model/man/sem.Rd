% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/factors.R
\name{sem}
\alias{sem}
\title{Confirmatory factor analysis / structural equation model}
\usage{
sem(data = NULL, model, ...)
}
\arguments{
\item{data}{A data frame. If omitted, the session default is used.}

\item{model}{A model description in
\href{https://lavaan.ugent.be/tutorial/syntax1.html}{lavaan syntax}, e.g.
\code{"trust =~ trust + service + support"}.}

\item{...}{Passed to \code{\link[lavaan:sem]{lavaan::sem()}}.}
}
\value{
An \code{ezrmodel_sem} object: \code{fit_measures} (chi-square, CFI, TLI, RMSEA,
SRMR, BIC), \code{paths} (standardised estimates with p-values), and the fitted
\code{lavaan} object. Has \code{print()} and \code{tidy()} methods.
}
\description{
Fits a measurement or structural model you specify in lavaan syntax and
returns the fit measures (with a plain-language read) and the standardised
paths in one object -- the confirmatory companion to \code{\link[=factors]{factors()}}.
}
\details{
Needs the suggested \code{lavaan} package. Latent variables are declared with \verb{=~}
(measured by), regressions with \code{~}, and covariances with \verb{~~}. The reported
fit measures come with rough targets: CFI/TLI \verb{>= 0.95}, RMSEA \verb{<= 0.06} and
SRMR \verb{<= 0.08} indicate good fit. \code{tidy()} returns the standardised paths so
coefficients are comparable across the model. Use \code{\link[=factors]{factors()}} first to
discover a structure, then \code{sem()} to confirm it.
}
\examples{
\dontshow{if (requireNamespace("lavaan", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
model <- "satisfaction =~ quality + value + service + ease +
                           support + trust + price + innovation"
sem(nps_drivers, model)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=factors]{factors()}}, \code{\link[=reliability]{reliability()}}.

Other factors:
\code{\link[=factors]{factors()}},
\code{\link[=reliability]{reliability()}}
}
\concept{factors}
