% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{ezrmodel_options}
\alias{ezrmodel_options}
\title{Get or set ezrmodel defaults}
\usage{
ezrmodel_options(...)
}
\arguments{
\item{...}{Either nothing (to read all values) or named \code{option = value}
pairs to set.}
}
\value{
When reading, a named list of all current values. When setting, the
previous values, invisibly.
}
\description{
ezrmodel reads a handful of defaults from R options (prefixed \code{ezrmodel.}), so
you can tune behaviour once instead of repeating arguments. Call with no
arguments to see the current effective values; call with \code{name = value} pairs
to set them for the session. Persist them across sessions with a YAML profile
(see \code{\link[=use_ezrmodel_profile]{use_ezrmodel_profile()}}).
}
\details{
Recognised options:
\describe{
\item{\code{na_answers}}{Strings treated as non-answers by \code{\link[=na_blank]{na_blank()}}.}
\item{\code{default_by}}{Default grouping column name(s) for grouped helpers.}
\item{\code{seed}}{Seed applied by stochastic methods (kmeans, random forest)
when set, for reproducibility.}
\item{\code{scale}}{Whether \code{\link[=cluster]{cluster()}} / \code{\link[=reduce_dims]{reduce_dims()}} standardise variables
by default.}
}
}
\examples{
ezrmodel_options()                  # view all defaults
ezrmodel_options(seed = 42)
ezrmodel_options()$seed             # 42
reset_ezrmodel_options()            # back to defaults
}
\seealso{
\code{\link[=use_ezrmodel_profile]{use_ezrmodel_profile()}}, \code{\link[=reset_ezrmodel_options]{reset_ezrmodel_options()}}.

Other config:
\code{\link[=edit_ezrmodel_profile]{edit_ezrmodel_profile()}},
\code{\link[=ezrmodel_profile_paths]{ezrmodel_profile_paths()}},
\code{\link[=get_dataset]{get_dataset()}},
\code{\link[=load_ezrmodel_profile]{load_ezrmodel_profile()}},
\code{\link[=reset_ezrmodel_options]{reset_ezrmodel_options()}},
\code{\link[=save_ezrmodel_profile]{save_ezrmodel_profile()}},
\code{\link[=use_dataset]{use_dataset()}},
\code{\link[=use_ezrmodel_profile]{use_ezrmodel_profile()}}
}
\concept{config}
