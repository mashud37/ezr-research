% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tests.R
\name{test_groups}
\alias{test_groups}
\title{Test a value across groups}
\usage{
test_groups(
  data = NULL,
  value,
  group,
  mu = NULL,
  ref = NULL,
  method = c("t", "wilcox"),
  alpha = 0.05
)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{value}{The numeric column to test (unquoted).}

\item{group}{The grouping column (unquoted).}

\item{mu}{Benchmark value for a one-sample test of each group. If both \code{mu}
and \code{ref} are \code{NULL}, \code{mu} defaults to the overall mean.}

\item{ref}{Reference group level for a two-sample test of every other group
against it. Overrides \code{mu}.}

\item{method}{\code{"t"} (Welch t-test, default) or \code{"wilcox"} (Mann-Whitney /
signed-rank, rank-based).}

\item{alpha}{Significance threshold for the \code{signif} flag. Default \code{0.05}.}
}
\value{
An \code{ezrmodel_tests} object wrapping a tidy table (\code{group},
\code{comparison}, \code{n}, \code{estimate}, \code{statistic}, \code{p_value}, \code{signif}). Has
\code{print()}, \code{tidy()} and \code{plot()} methods.
}
\description{
Runs a hypothesis test for each level of a grouping variable in one call and
returns a tidy significance table -- either each group against a \strong{benchmark}
\code{mu}, or each group against a \strong{reference} group. Replaces the hand-written
loop of \code{t.test()} / \code{wilcox.test()} calls.
}
\details{
Use \code{mu} to ask "is this group above/below a target?" (e.g. an NPS benchmark),
and \code{ref} to ask "does this group differ from our baseline group?". The
\code{estimate} is the group mean minus the benchmark (one-sample) or minus the
reference-group mean (two-sample). Blanks / non-answers in \code{group} and missing
values are dropped; results are sorted by p-value. \code{"wilcox"} is the robust
choice for skewed or ordinal values.
}
\examples{
test_groups(nps_drivers, nps, region, mu = 6)
#> Tests of 'nps' across 'region' (t, vs mu = 6)
test_groups(nps_drivers, nps, region, ref = "Europe")
}
\seealso{
\code{\link[=correlations]{correlations()}}.
}
\concept{tests}
