% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/factors.R
\name{factors}
\alias{factors}
\title{Exploratory factor analysis}
\usage{
factors(data = NULL, vars = NULL, n = NULL, rotate = "oblimin", scores = TRUE)
}
\arguments{
\item{data}{A data frame. If omitted, the session default is used.}

\item{vars}{The item columns (tidyselect). Default: all numeric columns.}

\item{n}{Number of factors. If \code{NULL} (default), chosen by parallel analysis.}

\item{rotate}{Rotation passed to \code{\link[psych:fa]{psych::fa()}}. Default \code{"oblimin"} (allows
correlated factors).}

\item{scores}{Append factor scores via \code{\link[=augment]{augment()}}? Default \code{TRUE}.}
}
\value{
An \code{ezrmodel_factors} object: \code{loadings} (item x factor), \code{variance}
(proportion explained per factor), \code{fit} (TLI, RMSEA, RMSR), the fitted
\code{psych} object and, when \code{scores = TRUE}, the factor \code{scores}. Has
\code{print()}, \code{plot()} (loadings heatmap), \code{tidy()} and \code{augment()} methods.
}
\description{
Finds the latent dimensions behind a set of correlated items: how many there
are, which items load on each, and how much variance they explain -- in one
object, with the number of factors chosen for you if you do not say.
}
\details{
Needs the suggested \code{psych} package. When \code{n = NULL}, \code{\link[psych:fa.parallel]{psych::fa.parallel()}}
suggests the number of factors by comparing the data's eigenvalues to those of
random data. Loadings are the correlations between items and factors; read each
factor by its high-loading items. Oblimin rotation lets factors correlate,
which is usually realistic for survey constructs. Rough fit guides: TLI
\verb{>= 0.95} and RMSEA \verb{<= 0.06} are good. \code{augment()} appends each respondent's
factor scores (\code{.MR1}, \code{.MR2}, ...).
}
\examples{
\dontshow{if (requireNamespace("psych", quietly = TRUE)) withAutoprint(\{ # examplesIf}
f <- factors(nps_drivers, vars = c(value, quality, service, ease,
                                   support, trust, price, innovation), n = 2)
f
tidy(f)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=reliability]{reliability()}}, \code{\link[=reduce_dims]{reduce_dims()}} for unsupervised PCA, \code{\link[=sem]{sem()}}.

Other factors:
\code{\link[=reliability]{reliability()}},
\code{\link[=sem]{sem()}}
}
\concept{factors}
