% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlate.R
\name{correlations}
\alias{correlations}
\title{Correlations among numeric variables (optionally vs a target)}
\usage{
correlations(
  data = NULL,
  target = NULL,
  method = c("spearman", "pearson", "kendall"),
  select = NULL
)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{target}{Optional outcome column (unquoted) to focus on; the result then
ranks variables by their correlation with it.}

\item{method}{\code{"spearman"} (default, rank-based and robust), \code{"pearson"} or
\code{"kendall"}.}

\item{select}{Columns to include (tidyselect). Default: all numeric columns.}
}
\value{
An \code{ezrmodel_cor} object: the correlation \code{matrix}, the \code{method}, and
(if a target was given) a ranked \code{target_cor} table. Has \code{print()},
\code{plot()} (heatmap) and \code{tidy()} methods.
}
\description{
Computes a correlation matrix over the numeric columns in one line, and -- when
you name a \code{target} -- a ranked table of how each variable correlates with it.
}
\details{
Spearman is the default because survey and behavioural variables are often
ordinal or skewed, and rank correlation does not assume linearity. Missing
values are handled pairwise. With a \code{target}, \code{print()} and \code{tidy()} give the
focused ranking that usually answers "what moves with the outcome?"; without
one you get the full matrix and its strongest pairs.
}
\examples{
correlations(nps_drivers, nps)
#> Correlations with 'nps' (spearman, 8 variables)
#>   quality  0.51
#>   value    0.46
#>   ...
}
\seealso{
\code{\link[=drivers]{drivers()}}, \code{\link[=model_lm]{model_lm()}}.
}
\concept{correlation}
