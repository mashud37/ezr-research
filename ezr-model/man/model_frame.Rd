% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep.R
\name{model_frame}
\alias{model_frame}
\title{Assemble a numeric model frame (target + predictors)}
\usage{
model_frame(data = NULL, target, predictors = NULL, na_rm = TRUE)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{target}{The outcome column (unquoted).}

\item{predictors}{Predictor columns, using tidyselect. If \code{NULL} (default),
every numeric column except \code{target} is used.}

\item{na_rm}{Drop rows with any missing value (complete cases). Default
\code{TRUE}.}
}
\value{
A \link[tibble:tibble]{tibble} with \code{target} first, then the numeric
predictors.
}
\description{
Builds the tidy, complete-case numeric data frame the modelling helpers need:
a target column followed by its predictors, with text salvaged to numbers and
incomplete rows dropped. This is the shared first step of \code{\link[=drivers]{drivers()}},
\code{\link[=model_lm]{model_lm()}} and \code{\link[=correlations]{correlations()}} -- exposed so you can inspect or reuse it.
}
\details{
When \code{predictors = NULL} only columns that are \emph{already numeric} are taken, so
id and text columns are ignored automatically. When you name predictors
explicitly they are passed through \code{\link[=ensure_numeric]{ensure_numeric()}} (so a worded rating
column still works), and any that come out entirely \code{NA} are dropped. The
target is also coerced with \code{\link[=ensure_numeric]{ensure_numeric()}}.
}
\examples{
model_frame(nps_drivers, nps)
#> # A tibble: 600 x 9
#>     nps value quality service  ease support trust price innovation
#>   <dbl> <dbl>   <dbl>   <dbl> <dbl>   <dbl> <dbl> <dbl>      <dbl>
#> # ... region / segment (non-numeric) are dropped automatically
}
\seealso{
\code{\link[=to_matrix]{to_matrix()}}, \code{\link[=drivers]{drivers()}}, \code{\link[=model_lm]{model_lm()}}.

Other prep:
\code{\link[=ensure_numeric]{ensure_numeric()}},
\code{\link[=na_blank]{na_blank()}},
\code{\link[=presence_matrix]{presence_matrix()}},
\code{\link[=to_matrix]{to_matrix()}}
}
\concept{prep}
