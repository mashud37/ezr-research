% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{topics}
\alias{topics}
\title{Fit a topic model to a text column}
\usage{
topics(
  data = NULL,
  text,
  id = NULL,
  k = 5,
  stopwords = NULL,
  min_count = 2,
  ...
)
}
\arguments{
\item{data}{A data frame. If omitted, the session default is used.}

\item{text}{The text column (unquoted).}

\item{id}{Optional document id column (unquoted).}

\item{k}{Number of topics. Default \code{5}.}

\item{stopwords}{Passed to the tokeniser (see \code{\link[=tokenize_text]{tokenize_text()}}).}

\item{min_count}{Drop terms appearing in fewer than this many documents
(trims the long tail). Default \code{2}.}

\item{...}{Passed to \code{\link[stm:stm]{stm::stm()}}.}
}
\value{
An \code{ezrmodel_topics} object: \code{beta} (term-topic weights), \code{gamma}
(document-topic proportions), \code{top_terms} (the defining terms per topic),
the fitted \code{stm} model and the term/document maps. Has \code{print()}, \code{plot()}
(top terms per topic), \code{tidy()} (beta) and \code{augment()} (each document's
most likely topic) methods.
}
\description{
Discovers \code{k} latent topics in a corpus and returns, in one object, the top
terms that define each topic (the \code{beta} matrix) and each document's topic
mixture (the \code{gamma}/theta matrix) -- ready to read, plot or join back to
respondent attributes.
}
\details{
Topic modelling needs the suggested \code{quanteda} and \code{stm} packages; without
them the function stops with an install hint. Text is tokenised and turned
into a document-feature matrix, rare terms are trimmed (\code{min_count}), and a
Structural Topic Model is fit with spectral initialisation (deterministic).
\code{beta} gives each term's weight in each topic; \code{gamma} gives each document's
share of each topic. Use \code{\link[=tidy]{tidy()}} for the term weights and \code{\link[=augment]{augment()}} to
attach the dominant topic back to your rows.
}
\examples{
\dontshow{if (requireNamespace("quanteda", quietly = TRUE) && requireNamespace("stm", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
tm <- topics(reviews, text, review_id, k = 3)
tm
tidy(tm)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=term_freq]{term_freq()}}, \code{\link[=tokenize_text]{tokenize_text()}}.

Other text:
\code{\link[=summarise_text]{summarise_text()}},
\code{\link[=term_freq]{term_freq()}},
\code{\link[=tokenize_text]{tokenize_text()}}
}
\concept{text}
