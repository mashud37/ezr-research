% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drivers.R
\name{drivers}
\alias{drivers}
\title{Find the drivers of a target variable}
\usage{
drivers(
  data = NULL,
  target,
  predictors = NULL,
  methods = c("cor", "lm", "rwa", "rf", "fa")
)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{target}{The outcome column (unquoted).}

\item{predictors}{Predictor columns (tidyselect). Default: all numeric columns
except \code{target}.}

\item{methods}{Which importance methods to combine, any of \code{"cor"}
(correlation), \code{"lm"} (standardised regression coefficients), \code{"rwa"}
(relative weights), \code{"rf"} (random-forest importance), \code{"fa"} (single-factor
loading). Defaults to all five. Methods whose package is not installed are
skipped with a message.}
}
\value{
An \code{ezrmodel_drivers} object: a list with the \code{target}, the \code{methods}
used, a long \code{importance} table (variable x method, with per-method rank),
and a \code{consensus} table (mean rank and mean normalised importance per
variable). Has \code{print()}, \code{plot()} and \code{tidy()} methods.
}
\description{
The signature ezrmodel helper: in one line, rank how strongly each predictor
relates to a \code{target}, by \strong{several methods at once}, and combine them into a
consensus. Different methods see different things (linear vs non-linear,
marginal vs partial, shared factor), so agreement across them is a robust
signal of what really drives the outcome.
}
\details{
Each method scores every predictor; scores are turned into per-method ranks
(1 = strongest) and a normalised 0-1 importance. The \strong{consensus} orders
variables by their mean rank across the available methods -- a variable that
is near the top everywhere rises, one that only one method likes does not. The
built-in methods cover complementary views: \code{cor} (simple monotonic
association), \code{lm} (unique linear contribution), \code{rwa} (variance shared out
between correlated predictors), \code{rf} (non-linear / interaction importance) and
\code{fa} (loading on the common factor). Optional methods (\code{rwa}/\code{rf}/\code{fa}) need
their Suggested package; without it they are dropped.
}
\examples{
d <- drivers(nps_drivers, nps, methods = c("cor", "lm"))
d
tidy(d)
}
\seealso{
\code{\link[=model_lm]{model_lm()}}, \code{\link[=correlations]{correlations()}}, \code{\link[=model_frame]{model_frame()}}.
}
\concept{drivers}
