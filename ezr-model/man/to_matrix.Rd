% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep.R
\name{to_matrix}
\alias{to_matrix}
\title{Build a numeric matrix for clustering or PCA}
\usage{
to_matrix(data = NULL, cols = NULL, scale = TRUE, id = NULL)
}
\arguments{
\item{data}{A data frame. If omitted, the session default is used.}

\item{cols}{Columns to include, using tidyselect. If \code{NULL} (default), all
numeric columns are used.}

\item{scale}{Standardise each column to mean 0 / sd 1. Default \code{TRUE} -- so
variables on different scales (e.g. spend vs counts) contribute equally.}

\item{id}{Optional column to use as row names (unquoted).}
}
\value{
A numeric matrix.
}
\description{
Selects numeric columns into a matrix, optionally standardised and with row
names taken from an id column -- the input shape \code{\link[=cluster]{cluster()}} and
\code{\link[=reduce_dims]{reduce_dims()}} expect.
}
\details{
Standardising matters: without it, a high-variance variable (like spend)
dominates the distances. Non-numeric columns among \code{cols} are dropped. Keep an
id via \code{id} so you can trace rows back after clustering.
}
\examples{
m <- to_matrix(ecommerce, c(recency_days, frequency, monetary),
               id = customer_id)
dim(m)
#> [1] 800   3
}
\seealso{
\code{\link[=cluster]{cluster()}}, \code{\link[=reduce_dims]{reduce_dims()}}.

Other prep:
\code{\link[=ensure_numeric]{ensure_numeric()}},
\code{\link[=model_frame]{model_frame()}},
\code{\link[=na_blank]{na_blank()}},
\code{\link[=presence_matrix]{presence_matrix()}}
}
\concept{prep}
