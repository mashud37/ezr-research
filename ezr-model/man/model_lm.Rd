% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{model_lm}
\alias{model_lm}
\alias{model_glm}
\title{Fit a linear or generalised linear model, tidily}
\usage{
model_lm(data = NULL, formula, ...)

model_glm(data = NULL, formula, family = stats::gaussian(), ...)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used. (Note: because \code{formula} is the second argument, with a default
dataset call \code{model_lm(formula = y ~ .)} or pipe the data in.)}

\item{formula}{A model formula, e.g. \code{nps ~ quality + value}.}

\item{...}{Passed to \code{\link[stats:lm]{stats::lm()}} / \code{\link[stats:glm]{stats::glm()}}.}

\item{family}{For \code{model_glm()}, the error family (e.g. \code{binomial()}).
Defaults to \code{gaussian()}.}
}
\value{
An \code{ezrmodel_fit} object (\code{fit}, \code{coefficients}, \code{glance}).
}
\description{
One-line \code{lm()} / \code{glm()} that returns a result object carrying the tidy
coefficient table, the fit summary and the model itself -- ready to \code{print()},
\code{plot()} (a coefficient plot), \code{tidy()} and \code{augment()}.
}
\details{
The coefficient table flags significance at p < 0.05; \code{glance} reports
R-squared / adjusted R-squared (lm) or deviance (glm) plus AIC/BIC and the
sample size. \code{augment()} returns the data with \code{.fitted} and \code{.resid}
appended. For \emph{ranking} many candidate predictors rather than fitting one
model, use \code{\link[=drivers]{drivers()}}.
}
\examples{
fit <- model_lm(nps_drivers, nps ~ quality + value + service)
fit
tidy(fit)
# logistic model of "promoter" status
g <- model_glm(nps_drivers, I(nps >= 9) ~ quality + value,
               family = binomial())
tidy(g)
}
\seealso{
\code{\link[=drivers]{drivers()}}, \code{\link[=correlations]{correlations()}}.

Other regression:
\code{\link[=model_select]{model_select()}}
}
\concept{regression}
