% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{tokenize_text}
\alias{tokenize_text}
\title{Tokenise a text column into a tidy term table}
\usage{
tokenize_text(data = NULL, text, id = NULL, stopwords = NULL, min_chars = 3)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{text}{The text column (unquoted).}

\item{id}{Optional document id column (unquoted). If omitted, the row number
is used.}

\item{stopwords}{Stop-word handling: \code{NULL} (default) uses the \code{stopwords}
package when installed (English), otherwise a small built-in fallback; a
character vector supplies your own list; \code{FALSE} disables removal. Pass e.g.
\code{stopwords::stopwords("de")} for another language.}

\item{min_chars}{Drop tokens shorter than this many characters. Default \code{3}.}
}
\value{
An \code{ezrmodel_tokens} object wrapping a tidy \code{tibble} of \code{doc}/\code{term}
(plus the id column). Has \code{print()}, \code{plot()} (top terms) and \code{tidy()}
methods.
}
\description{
Turns a free-text column into one row per word, lower-cased and cleaned, with
punctuation, very short tokens and stop-words removed. This is the building
block the rest of the text helpers (\code{\link[=term_freq]{term_freq()}}, \code{\link[=topics]{topics()}}) sit on, but it
is useful on its own for a quick word count or to feed your own dplyr summary.
}
\details{
Cleaning is deliberately simple and dependency-free: lower-case, strip
anything that is not a letter, digit, space or apostrophe, split on
whitespace, drop tokens under \code{min_chars} and remove stop-words. Keep an \code{id}
so you can join term-level results back to respondent attributes (e.g. an NPS
group) for downstream analysis.
}
\examples{
tk <- tokenize_text(reviews, text, review_id)
tk
tidy(tk)
}
\seealso{
\code{\link[=term_freq]{term_freq()}}, \code{\link[=topics]{topics()}}.

Other text:
\code{\link[=summarise_text]{summarise_text()}},
\code{\link[=term_freq]{term_freq()}},
\code{\link[=topics]{topics()}}
}
\concept{text}
