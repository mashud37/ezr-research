% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep.R
\name{presence_matrix}
\alias{presence_matrix}
\title{Build a presence/absence matrix from a delimited column}
\usage{
presence_matrix(data = NULL, id, items, delim = ",", min_count = NULL)
}
\arguments{
\item{data}{A data frame. If omitted, the session default is used.}

\item{id}{The record id column (unquoted).}

\item{items}{The delimited column (unquoted).}

\item{delim}{Item separator. Default \code{","}.}

\item{min_count}{Optional minimum overall frequency: items appearing fewer
than \code{min_count} times are dropped (useful to trim a long tail). Default
\code{NULL} (keep all).}
}
\value{
A \link[tibble:tibble]{tibble} with the id column plus one 0/1 column per
item.
}
\description{
Turns a "check-all" / tags / resources column -- one row per record, several
comma-separated items per cell -- into a wide 0/1 table (one column per item),
the natural input for clustering items or records by co-occurrence.
}
\details{
Items are split on \code{delim}, trimmed, counted, optionally frequency-filtered,
then pivoted wide with absent items filled \code{0}. Pass the result to
\code{\link[=to_matrix]{to_matrix()}} (with \code{scale = FALSE}) and then \code{\link[=cluster]{cluster()}} with a binary /
Jaccard distance to group records by what they share.
}
\examples{
df <- tibble::tibble(id = c("a", "b", "c"),
                     tags = c("x,y", "y,z", "x,z,w"))
presence_matrix(df, id, tags)
#> # A tibble: 3 x 5  (id, x, y, z, w as 0/1)
}
\seealso{
\code{\link[=to_matrix]{to_matrix()}}, \code{\link[=cluster]{cluster()}}.

Other prep:
\code{\link[=ensure_numeric]{ensure_numeric()}},
\code{\link[=model_frame]{model_frame()}},
\code{\link[=na_blank]{na_blank()}},
\code{\link[=to_matrix]{to_matrix()}}
}
\concept{prep}
