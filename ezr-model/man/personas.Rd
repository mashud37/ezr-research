% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{personas}
\alias{personas}
\title{Simulated consumer personas (a clean clustering example)}
\format{
A \link[tibble:tibble]{tibble} with 740 rows and 9 columns:
\describe{
\item{customer_id}{Customer id (character).}
\item{spend_index}{Monthly spend index (0-120).}
\item{visit_freq}{Visits per month (integer).}
\item{basket_size}{Average items per basket.}
\item{discount_sensitivity}{Responsiveness to discounts (0-100).}
\item{loyalty_score}{Loyalty-programme engagement (0-100).}
\item{browse_minutes}{Average session length in minutes.}
\item{pages_viewed}{Pages viewed per session (redundant with
\code{browse_minutes}).}
\item{persona}{Ground-truth segment label (character).}
}
}
\source{
Simulated. See \code{data-raw/make_datasets.R}.
}
\usage{
personas
}
\description{
A fictional customer table built from explicit mathematical functions so that
every clustering technique in the package gives a textbook "what good looks
like" result. Five personas are drawn as well-separated Gaussian blobs (via
\code{MASS::mvrnorm}) in six informative dimensions -- the between-cluster distance
is far larger than the within-cluster spread -- so \code{\link[=cluster]{cluster()}} with \code{"kmeans"},
\code{"hclust"} or \code{"pam"} all recover the same segments, the silhouette peaks at
the true \code{k = 5} (so \code{cluster(personas)} with \code{k = NULL} finds it), and
\code{\link[=reduce_dims]{reduce_dims()}} (PCA / UMAP / t-SNE) shows clean structure. A redundant column
(\code{pages_viewed}, which tracks \code{browse_minutes}) gives PCA an obvious low-rank
structure without disturbing the segments. The ground-truth label is kept in
\code{persona} so you can check a recovered solution with \code{\link[=cluster_profile]{cluster_profile()}}.
}
\examples{
\dontshow{if (requireNamespace("cluster", quietly = TRUE)) withAutoprint(\{ # examplesIf}
cl <- cluster(personas, vars = spend_index:browse_minutes)
cl
cluster_profile(cl, persona)
\dontshow{\}) # examplesIf}
}
\seealso{
Other data:
\code{\link{ecommerce}},
\code{\link{nps_drivers}},
\code{\link{reviews}}
}
\concept{data}
\keyword{datasets}
