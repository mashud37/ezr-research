% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare_models}
\alias{compare_models}
\title{Compare fitted models side by side}
\usage{
compare_models(..., metric = c("aic", "bic", "rmse", "r2"))
}
\arguments{
\item{...}{Two or more fitted models: \code{\link[=model_lm]{model_lm()}} / \code{\link[=model_glm]{model_glm()}} results,
\code{\link[=model_select]{model_select()}} results, or raw \code{lm} / \code{glm} objects. Name them to label
the rows (e.g. \verb{simple = fit1, full = fit2}).}

\item{metric}{The metric to sort by and judge "best": \code{"aic"} (default),
\code{"bic"}, \code{"rmse"} (all lower-is-better) or \code{"r2"} (higher-is-better).}
}
\value{
An \code{ezrmodel_comparison} object wrapping a \code{table} (one row per model)
ordered best-first. Has \code{print()}, \code{plot()} and \code{tidy()} methods.
}
\description{
Lines up several models in one table of comparable fit statistics -- sample
size, degrees of freedom, R-squared, AIC, BIC and RMSE -- and marks the best
on your chosen metric, so picking between candidates is a glance rather than a
hunt through separate summaries.
}
\details{
Metrics are computed on a common footing: R-squared is the usual one for \code{lm}
and a deviance-based pseudo-R-squared for \code{glm}; RMSE is the root-mean-square
residual; AIC/BIC come from the model likelihood (and are \code{NA} for penalised
\code{glmnet} fits, which have no comparable likelihood -- compare those on RMSE or
R-squared). AIC and BIC reward fit while penalising complexity, BIC more
harshly; agreement across metrics is reassuring. Compare models fit to the
\emph{same rows} for the numbers to mean anything.
}
\examples{
a <- model_lm(nps_drivers, nps ~ quality + value)
b <- model_lm(nps_drivers, nps ~ quality + value + service + trust)
compare_models(simple = a, fuller = b)
}
\seealso{
\code{\link[=model_lm]{model_lm()}}, \code{\link[=model_select]{model_select()}}.
}
\concept{compare}
