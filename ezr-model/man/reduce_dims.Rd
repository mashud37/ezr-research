% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reduce.R
\name{reduce_dims}
\alias{reduce_dims}
\title{Reduce dimensions with PCA, UMAP or t-SNE}
\usage{
reduce_dims(
  data = NULL,
  method = "pca",
  n = NULL,
  vars = NULL,
  scale = ezrmodel_default("scale"),
  ...
)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{method}{\code{"pca"} (default, principal components), \code{"umap"} (uniform
manifold approximation, needs \code{uwot}) or \code{"tsne"} (t-SNE, needs \code{Rtsne}).}

\item{n}{Number of components / embedding dimensions. For \code{"pca"}, \code{NULL}
(default) keeps all; for \code{"umap"}/\code{"tsne"} the default is \code{2}.}

\item{vars}{Columns to reduce (tidyselect). Default: all numeric columns.}

\item{scale}{Standardise variables first. Default: the \code{scale} option
(\code{TRUE}).}

\item{...}{Passed to the engine (\code{\link[stats:prcomp]{stats::prcomp()}}, \code{\link[uwot:umap]{uwot::umap()}} or
\code{\link[Rtsne:Rtsne]{Rtsne::Rtsne()}}).}
}
\value{
An \code{ezrmodel_reduction} object: \code{scores} (the components / embedding),
and for PCA also \code{loadings} and \code{variance}. Has \code{print()}, \code{plot()} (scree
for PCA, a 2-D scatter for embeddings), \code{tidy()} and \code{augment()} methods.
}
\description{
One-line dimensionality reduction. \code{"pca"} returns the scores, variable
loadings and variance explained -- the complete linear picture. \code{"umap"} and
\code{"tsne"} return a low-dimensional embedding that preserves local structure,
ideal for visualising clusters in two dimensions.
}
\details{
Standardising (the default) means each variable contributes equally regardless
of its units. \strong{PCA} is linear and interpretable: the \code{variance} table tells
you how many components are worth keeping and \code{loadings} say what each
component means. \strong{UMAP} and \strong{t-SNE} are non-linear and built for \emph{seeing}
structure -- they place similar rows near each other in 2-D but their axes have
no global meaning, so use them to visualise (often coloured by a \code{\link[=cluster]{cluster()}}
assignment), not to interpret coordinates. Both are stochastic and seeded from
the \code{seed} option for reproducibility. \code{augment()} appends the kept dimensions
(\code{.PC1}, \code{.UMAP1}, ...) to the rows used.
}
\examples{
r <- reduce_dims(ecommerce, vars = c(recency_days, frequency, monetary,
                                     tenure_months, returns))
r
tidy(r)
}
\seealso{
\code{\link[=cluster]{cluster()}}, \code{\link[=to_matrix]{to_matrix()}}.
}
\concept{reduction}
