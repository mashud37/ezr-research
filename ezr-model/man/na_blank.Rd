% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{na_blank}
\alias{na_blank}
\title{Convert blank and boilerplate non-answers to \code{NA}}
\usage{
na_blank(x, also = ezrmodel_default("na_answers"), trim = TRUE)
}
\arguments{
\item{x}{A character (or factor) vector.}

\item{also}{Additional exact strings to treat as missing. Defaults to the
\code{na_answers} option (\code{"Prefer not to answer"}; see \code{\link[=ezrmodel_options]{ezrmodel_options()}}).
Set to \code{character(0)} to only blank out \code{""}.}

\item{trim}{Whether to trim surrounding whitespace before comparing.
Defaults to \code{TRUE}.}
}
\value{
A character vector the same length as \code{x}, with blanks and
non-answers replaced by \code{NA}.
}
\description{
Data exports are littered with empty strings and standard "non-answer"
options such as "Prefer not to answer". This helper turns those into real
\code{NA} values so they drop out of models and summaries, replacing the
repetitive \code{filter(x != "", x != "Prefer not to answer")} idiom.
}
\details{
The input is coerced to character (so factors are handled), optionally
whitespace-trimmed, and any value equal to \code{""} or to one of \code{also} becomes
\code{NA}. The default \code{also} reads the \code{na_answers} option, so you can change what
counts as a non-answer package-wide with
\code{ezrmodel_options(na_answers = ...)}. The modelling helpers call \code{na_blank()}
for you when cleaning categorical columns.
}
\examples{
na_blank(c("Yes", "", "Prefer not to answer", "No"))
#> [1] "Yes" NA    NA    "No"

na_blank(c("a", "n/a", "N/A"), also = c("n/a", "N/A"))
#> [1] "a" NA  NA
}
\seealso{
\code{\link[=ensure_numeric]{ensure_numeric()}}, \code{\link[=ezrmodel_options]{ezrmodel_options()}}.

Other prep:
\code{\link[=ensure_numeric]{ensure_numeric()}},
\code{\link[=model_frame]{model_frame()}},
\code{\link[=presence_matrix]{presence_matrix()}},
\code{\link[=to_matrix]{to_matrix()}}
}
\concept{prep}
