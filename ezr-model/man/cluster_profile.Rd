% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{cluster_profile}
\alias{cluster_profile}
\title{Cross clusters against another variable}
\usage{
cluster_profile(clusters, by, digits = 0)
}
\arguments{
\item{clusters}{An \code{\link[=cluster]{cluster()}} result (\code{ezrmodel_clusters}).}

\item{by}{The variable to cross against (unquoted), from the clustered data.}

\item{digits}{Decimal places for the percentages. Default \code{0}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per \code{by} level and one column
per cluster, holding column percentages.
}
\description{
Composes a cluster solution against a categorical variable as a percentage
crosstab -- e.g. "what share of each region falls in each cluster?" -- to give
the clusters a real-world reading.
}
\details{
Percentages are computed within each \code{by} level (each row sums to ~100), so
you read "of this segment, X\% are in cluster k". Blanks / non-answers in \code{by}
are dropped.
}
\examples{
\dontshow{if (requireNamespace("cluster", quietly = TRUE)) withAutoprint(\{ # examplesIf}
cl <- cluster(ecommerce, k = 3,
              vars = c(recency_days, frequency, monetary))
cluster_profile(cl, region)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=cluster]{cluster()}}.

Other clustering:
\code{\link[=cluster]{cluster()}}
}
\concept{clustering}
