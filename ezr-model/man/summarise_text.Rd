% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{summarise_text}
\alias{summarise_text}
\title{Extractive summary of a text column}
\usage{
summarise_text(data = NULL, text, n = 5, ...)
}
\arguments{
\item{data}{A data frame. If omitted, the session default is used.}

\item{text}{The text column (unquoted).}

\item{n}{Number of sentences to return. Default \code{5}.}

\item{...}{Passed to \code{\link[lexRankr:lexRank]{lexRankr::lexRank()}}.}
}
\value{
A \code{tibble} of the top \code{n} sentences with their LexRank \code{score},
ordered most-central first.
}
\description{
Picks the most central sentences from a body of text (LexRank, a PageRank over
sentence similarity) -- a quick, faithful summary of "what are people saying?"
that uses the respondents' own words rather than a generative paraphrase.
}
\details{
Needs the suggested \code{lexRankr} package. All non-empty cells of \code{text} are
pooled, split into sentences, and ranked by how central each is to the others;
the top \code{n} are returned verbatim. This is \emph{extractive} (it selects real
sentences), so unlike \code{\link[=ai_summarise]{ai_summarise()}} it never invents wording and needs no
API key.
}
\examples{
\dontshow{if (requireNamespace("lexRankr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
summarise_text(reviews, text, n = 3)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=ai_summarise]{ai_summarise()}} for a generative summary.

Other text:
\code{\link[=term_freq]{term_freq()}},
\code{\link[=tokenize_text]{tokenize_text()}},
\code{\link[=topics]{topics()}}
}
\concept{text}
