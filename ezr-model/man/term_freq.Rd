% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{term_freq}
\alias{term_freq}
\title{Term frequencies and tf-idf}
\usage{
term_freq(
  data = NULL,
  text,
  by = NULL,
  weight = NULL,
  top = NULL,
  stopwords = NULL,
  min_chars = 3
)
}
\arguments{
\item{data}{A data frame. If omitted, the session default is used.}

\item{text}{The text column (unquoted).}

\item{by}{Optional grouping column (unquoted) -- each group is treated as a
document for tf-idf (e.g. an NPS group, a product, a rating).}

\item{weight}{\code{"tfidf"} (default) when \code{by} is given, else \code{"count"}. Use
\code{"count"} for plain frequencies.}

\item{top}{Keep only the top \code{top} terms (per group, when grouped). Default
\code{NULL} (keep all).}

\item{stopwords}{Passed to \code{\link[=tokenize_text]{tokenize_text()}}.}

\item{min_chars}{Passed to \code{\link[=tokenize_text]{tokenize_text()}}. Default \code{3}.}
}
\value{
An \code{ezrmodel_termfreq} object wrapping a tidy \code{tibble} with \code{term},
the grouping column (if any), the count \code{n}, and \code{tf}/\code{idf}/\code{tfidf} columns
when weighted. Has \code{print()}, \code{plot()} and \code{tidy()} methods.
}
\description{
Counts terms in a text column, optionally \emph{per group}, and (when a grouping
is given) weights them by tf-idf so the terms that are distinctive to each
group rise to the top -- the everyday "what words set these comments apart?"
question.
}
\details{
With no \code{by}, you get overall counts. With a \code{by} grouping, each group is a
document: \code{tf} is the within-group share of a term, \code{idf} is
\code{log(n_groups / n_groups_containing_term)}, and \code{tfidf = tf * idf}. A term
common to every group has \code{idf = 0} and drops out, leaving the words that
characterise each group. \code{plot()} shows the top terms, faceted by group when
grouped.
}
\examples{
# Distinctive words by star rating
tf <- term_freq(reviews, text, by = rating, top = 8)
tf
tidy(tf)
}
\seealso{
\code{\link[=tokenize_text]{tokenize_text()}}, \code{\link[=topics]{topics()}}.

Other text:
\code{\link[=summarise_text]{summarise_text()}},
\code{\link[=tokenize_text]{tokenize_text()}},
\code{\link[=topics]{topics()}}
}
\concept{text}
