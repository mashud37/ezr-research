% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regularise.R
\name{model_select}
\alias{model_select}
\title{Select predictors with stepwise or regularised regression}
\usage{
model_select(
  data = NULL,
  formula,
  method = c("stepwise", "lasso", "ridge", "elastic"),
  family = "gaussian",
  alpha = 0.5,
  nfolds = 10
)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{formula}{A model formula, e.g. \code{nps ~ value + quality + service}.}

\item{method}{One of \code{"stepwise"} (both-direction AIC selection on an
\code{lm}/\code{glm}), \code{"lasso"}, \code{"ridge"} or \code{"elastic"} (penalised \code{glmnet} fit,
with the penalty \code{lambda} chosen by cross-validation).}

\item{family}{Error family, as a string. Default \code{"gaussian"}. Use
\code{"binomial"} for a 0/1 outcome.}

\item{alpha}{Elastic-net mixing for \code{method = "elastic"} (0 = ridge,
1 = lasso). Default \code{0.5}. Ignored for the other methods.}

\item{nfolds}{Cross-validation folds for the penalised methods. Default \code{10}.}
}
\value{
An \code{ezrmodel_select} object: the \code{method}, the \code{coefficients}
(kept/non-zero terms), the \code{selected} term names, a \code{glance} of fit, and the
underlying model. Has \code{print()}, \code{plot()} (coefficients), \code{tidy()} and
\code{augment()} methods.
}
\description{
Fits a regression that \emph{chooses its own predictors}: stepwise selection by
AIC, or a penalised (lasso / ridge / elastic-net) fit that shrinks weak
coefficients -- in one line, returning the kept terms, their coefficients and
the fit, so you can see what survived.
}
\details{
Stepwise selection adds and drops terms to minimise AIC; it returns an
ordinary \code{lm}/\code{glm}, so the coefficient table keeps standard errors and
p-values. The penalised methods need the suggested \code{glmnet} package: they
standardise predictors, fit a path of \code{lambda} values, and report the
coefficients at the cross-validated \code{lambda.min}. \strong{Lasso} zeroes out weak
predictors (so \code{selected} is short), \strong{ridge} keeps all but shrinks them, and
\strong{elastic-net} blends the two. Penalised coefficients have no p-values by
design. The cross-validation is seeded from the \code{seed} option for
reproducibility. To rank predictors by several importance methods instead of
selecting one model, see \code{\link[=drivers]{drivers()}}; to compare fitted models, see
\code{\link[=compare_models]{compare_models()}}.
}
\examples{
\dontshow{if (requireNamespace("glmnet", quietly = TRUE)) withAutoprint(\{ # examplesIf}
las <- model_select(nps_drivers,
  nps ~ value + quality + service + ease + support + trust + price + innovation,
  method = "lasso")
las
tidy(las)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=model_lm]{model_lm()}}, \code{\link[=drivers]{drivers()}}, \code{\link[=compare_models]{compare_models()}}.

Other regression:
\code{\link[=model_lm]{model_lm()}}
}
\concept{regression}
