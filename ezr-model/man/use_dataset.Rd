% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{use_dataset}
\alias{use_dataset}
\title{Set a default dataset for the session}
\usage{
use_dataset(data)
}
\arguments{
\item{data}{A data frame to use as the default.}
}
\value{
\code{data}, invisibly (so it can sit in a pipe).
}
\description{
Registers a data frame as the session's default, so the analysis helpers can
be called without repeating \code{data} every time. Set it once at the top of a
script or Quarto document and then omit \code{data} (or pass it by pipe).
}
\details{
Because \code{data} is the first argument of the helpers and R evaluates it before
the function runs, a \emph{bare} positional call like \code{drivers(nps)} cannot fall
back to the default -- R would try to treat \code{nps} as the data. So, with a
default set, call the helpers in one of these equivalent ways:
\itemize{
\item pipe the data in once: \code{nps_drivers \%>\% drivers(nps)};
\item name the argument: \code{drivers(target = nps)};
\item or just keep passing \code{data} explicitly.
}

The default applies to the analysis helpers that read a raw dataset
(\code{drivers()}, \code{cluster()}, \code{correlations()}, \code{model_lm()}, \code{reduce_dims()},
\code{test_groups()}, ...). It does \strong{not} apply to helpers that consume an
already-computed result object. The setting lives only in the current R
session; clear it with \code{\link[=clear_dataset]{clear_dataset()}}.
}
\examples{
use_dataset(nps_drivers)
correlations(target = nps)            # data taken from the default
clear_dataset()
}
\seealso{
\code{\link[=get_dataset]{get_dataset()}}, \code{\link[=clear_dataset]{clear_dataset()}}, \code{\link[=has_dataset]{has_dataset()}}.

Other config:
\code{\link[=edit_ezrmodel_profile]{edit_ezrmodel_profile()}},
\code{\link[=ezrmodel_options]{ezrmodel_options()}},
\code{\link[=ezrmodel_profile_paths]{ezrmodel_profile_paths()}},
\code{\link[=get_dataset]{get_dataset()}},
\code{\link[=load_ezrmodel_profile]{load_ezrmodel_profile()}},
\code{\link[=reset_ezrmodel_options]{reset_ezrmodel_options()}},
\code{\link[=save_ezrmodel_profile]{save_ezrmodel_profile()}},
\code{\link[=use_ezrmodel_profile]{use_ezrmodel_profile()}}
}
\concept{config}
