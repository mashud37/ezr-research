% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keys.R
\name{set_llm_key}
\alias{set_llm_key}
\title{Store an LLM API key in the system keyring}
\usage{
set_llm_key(provider, key = NULL)
}
\arguments{
\item{provider}{Provider name, e.g. \code{"openai"}, \code{"anthropic"}, \code{"google"}.
Used as the keyring username.}

\item{key}{The API key. If \code{NULL} (default) and the session is interactive,
you are prompted for it without echoing to the console.}
}
\value{
Invisibly \code{TRUE} on success.
}
\description{
Saves a provider's API key to the OS credential store (via \pkg{keyring}),
namespaced under the \code{"ezrintelligence"} service. Keys stored this way are
picked up automatically by \code{\link[=ai_chat]{ai_chat()}} and \code{\link[=ai_summarise]{ai_summarise()}}, so secrets never
have to live in your scripts.
}
\details{
Keys are saved in the operating system's credential store via the suggested
\code{keyring} package, namespaced under the \code{"ezrintelligence"} service, so they
never appear in your scripts or history. \code{\link[=ai_chat]{ai_chat()}} and \code{\link[=ai_summarise]{ai_summarise()}}
look them up automatically (falling back to the conventional environment
variable, e.g. \code{OPENAI_API_KEY}). In an interactive session, omit \code{key} to be
prompted without echoing it to the console.
}
\examples{
\dontrun{
set_llm_key("openai")                 # prompts securely
set_llm_key("anthropic", Sys.getenv("ANTHROPIC_API_KEY"))
}
}
\seealso{
\code{\link[=get_llm_key]{get_llm_key()}}, \code{\link[=has_llm_key]{has_llm_key()}}, \code{\link[=delete_llm_key]{delete_llm_key()}},
\code{\link[=list_llm_keys]{list_llm_keys()}}.

Other keys:
\code{\link[=delete_llm_key]{delete_llm_key()}},
\code{\link[=get_llm_key]{get_llm_key()}},
\code{\link[=has_llm_key]{has_llm_key()}},
\code{\link[=list_llm_keys]{list_llm_keys()}}
}
\concept{keys}
