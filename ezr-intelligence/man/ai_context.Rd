% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{ai_context}
\alias{ai_context}
\title{Describe the study behind a table}
\usage{
ai_context(
  data = NULL,
  n = NULL,
  question = NULL,
  base = NULL,
  precision = NULL,
  fieldwork = NULL,
  notes = NULL
)
}
\arguments{
\item{data}{Optional raw data (one row per respondent). Used only to derive
\code{n} and, from it, the default \code{precision} note; the rows themselves are
never sent to a provider.}

\item{n}{Sample size. Derived from \code{data} when omitted.}

\item{question}{The verbatim question wording behind the table(s).}

\item{base}{Who was asked, e.g. \code{"All respondents"} or
\code{"Detractors only (n = 142)"}.}

\item{precision}{Character vector of precision notes. Derived from the
sample size when omitted. Pass your own (for instance the bullets from a
proper design-effect calculation) to override it.}

\item{fieldwork}{Fieldwork description, e.g. \code{"12-19 May 2026, online panel"}.}

\item{notes}{Any further context worth giving the model.}
}
\value{
An \code{ezrintelligence_context} list.
}
\description{
Bundles the facts a good analyst would want next to any table -- sample
size, question wording, base, precision -- so \code{\link[=ai_summarise]{ai_summarise()}} and
\code{\link[=ai_slide_text]{ai_slide_text()}} can pass them to the model alongside the figures. Given
the raw respondent-level \code{data}, the sample size and a margin-of-error note
are derived for you.
}
\details{
Context is what turns a generic summary into an analyst's summary: with a
sample size and margin of error present, the built-in templates flag
differences within the margin of error instead of narrating noise, and cite
the base alongside findings. Reuse one context object across every
\code{\link[=ai_summarise]{ai_summarise()}} call for the same study.

The derived precision note is the textbook worst case for a simple random
sample: the margin of error at \code{p = 0.5} and the confidence level set by the
\code{confidence_z} option. It ignores weighting, clustering and finite
populations, so pass \code{precision} yourself when you have a real design-effect
figure.
}
\examples{
ctx <- ai_context(
  n = 1000,
  question = "How likely are you to recommend us?",
  base = "All respondents",
  fieldwork = "12-19 May 2026, online panel"
)
ctx
#> Study context for AI summaries
#> Study context:
#> - Sample size: n = 1000
#> - Question asked: How likely are you to recommend us?
#> - Base: All respondents
#> - Fieldwork: 12-19 May 2026, online panel
#> - Precision: a percentage from the full sample carries a margin of error
#>   of about +/- 3.1 percentage points, ...
}
\seealso{
\code{\link[=ai_summarise]{ai_summarise()}}, \code{\link[=ai_report_sections]{ai_report_sections()}}.

Other ai:
\code{\link[=ai_chat]{ai_chat()}},
\code{\link[=ai_report_sections]{ai_report_sections()}},
\code{\link[=ai_slide_text]{ai_slide_text()}},
\code{\link[=ai_summarise]{ai_summarise()}}
}
\concept{ai}
