% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{ezrintelligence_options}
\alias{ezrintelligence_options}
\title{Get or set ezrintelligence defaults}
\usage{
ezrintelligence_options(...)
}
\arguments{
\item{...}{Either nothing (to read all values) or named \code{option = value}
pairs to set.}
}
\value{
When reading, a named list of all current values. When setting, the
previous values, invisibly.
}
\description{
ezrintelligence reads its defaults from R options (prefixed
\code{ezrintelligence.}), so you can pick a provider and model once instead of
repeating them on every call. Call with no arguments to see the current
effective values; call with \code{name = value} pairs to set them for the
session. Persist them across sessions with a YAML profile (see
\code{\link[=use_ezrintelligence_profile]{use_ezrintelligence_profile()}}).
}
\details{
Recognised options:
\describe{
\item{\code{provider}}{Default provider for \code{\link[=ai_chat]{ai_chat()}} and \code{\link[=ai_summarise]{ai_summarise()}}
(default \code{"openai"}).}
\item{\code{model}}{Default model name; \code{NULL} (default) uses the provider's
own default.}
\item{\code{template}}{Default prompt template for \code{\link[=ai_summarise]{ai_summarise()}} (default
\code{"key_findings"}).}
\item{\code{max_rows}}{Rows of a table included in a prompt before the rest are
summarised away as omitted (default \code{50}).}
\item{\code{confidence_z}}{z value behind the margin of error \code{\link[=ai_context]{ai_context()}}
derives from a sample size (default \code{1.96}, a 95\% interval).}
}

Options are ordinary R options under the \code{ezrintelligence.} prefix, so
anything you can do with \code{\link[=options]{options()}} works here too, and a setting lasts for
the session. Setting returns the previous values invisibly, so you can
restore them. To make settings permanent, write them to a YAML profile,
which is loaded automatically at package start-up.
}
\examples{
ezrintelligence_options()                    # view all defaults
ezrintelligence_options(provider = "anthropic")
ezrintelligence_options()$provider           # "anthropic"
reset_ezrintelligence_options()              # back to defaults
}
\seealso{
\code{\link[=use_ezrintelligence_profile]{use_ezrintelligence_profile()}}, \code{\link[=load_ezrintelligence_profile]{load_ezrintelligence_profile()}},
\code{\link[=reset_ezrintelligence_options]{reset_ezrintelligence_options()}}.

Other config:
\code{\link[=edit_ezrintelligence_profile]{edit_ezrintelligence_profile()}},
\code{\link[=ezrintelligence_profile_paths]{ezrintelligence_profile_paths()}},
\code{\link[=load_ezrintelligence_profile]{load_ezrintelligence_profile()}},
\code{\link[=reset_ezrintelligence_options]{reset_ezrintelligence_options()}},
\code{\link[=save_ezrintelligence_profile]{save_ezrintelligence_profile()}},
\code{\link[=use_ezrintelligence_profile]{use_ezrintelligence_profile()}}
}
\concept{config}
