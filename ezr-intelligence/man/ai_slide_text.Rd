% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slides.R
\name{ai_slide_text}
\alias{ai_slide_text}
\title{Draft a slide title and takeaway bullets for a table}
\usage{
ai_slide_text(
  data,
  title = NULL,
  want_title = TRUE,
  chat = NULL,
  provider = ezrintelligence_default("provider"),
  model = ezrintelligence_default("model"),
  context = NULL,
  max_rows = ezrintelligence_default("max_rows")
)
}
\arguments{
\item{data}{The summary table behind the slide.}

\item{title}{Optional working title for the table, given to the model as
context. Not returned unless \code{want_title = FALSE}.}

\item{want_title}{Draft a headline title as well as bullets. Defaults to
\code{TRUE}; set \code{FALSE} to keep your own titles and pay for one call instead
of two.}

\item{chat}{An existing \code{\link[=ai_chat]{ai_chat()}} object reused across slides. If \code{NULL},
one is created from \code{provider} / \code{model}.}

\item{provider, model}{Passed to \code{\link[=ai_chat]{ai_chat()}} when \code{chat} is not supplied.}

\item{context}{Optional study context from \code{\link[=ai_context]{ai_context()}}, given to the
model with the data.}

\item{max_rows}{Maximum rows of \code{data} to include. Defaults to the
\code{max_rows} option.}
}
\value{
A list with \code{title} (a length-1 string, or \code{NULL}) and \code{bullets} (a
character vector, or \code{NULL}). A failed call messages and returns \code{NULL} in
that slot, so a network hiccup degrades to a plain slide rather than
killing a deck build.
}
\description{
Asks the model for the two pieces of text a chart slide needs: a
headline-style title stating the takeaway, and three or four takeaway
bullets to sit beside the chart. Returns them as plain strings for you to
place in whatever deck builder you use.
}
\details{
The two \code{slide_title} and \code{slide_bullets} prompt templates carry the
house style: the title states a so-what rather than a topic, and each bullet
is a self-contained finding a presenter can say out loud. Pass one \code{chat}
across every slide in a deck to reuse the connection.

Everything is returned as text rather than written into a document, so it
drops into any deck builder: \code{officer::ph_with()} for a two-content layout,
\code{officer::set_notes()} for speaker notes, or a Quarto chunk that \code{cat()}s
the bullets.
}
\examples{
\dontrun{
gender <- data.frame(answer = c("Female", "Male"), pct = c(55, 45))
chat <- ai_chat()
text <- ai_slide_text(gender, title = "Gender", chat = chat)
text$title
text$bullets
}
}
\seealso{
\code{\link[=ai_summarise]{ai_summarise()}}, \code{\link[=ai_context]{ai_context()}}.

Other ai:
\code{\link[=ai_chat]{ai_chat()}},
\code{\link[=ai_context]{ai_context()}},
\code{\link[=ai_report_sections]{ai_report_sections()}},
\code{\link[=ai_summarise]{ai_summarise()}}
}
\concept{ai}
