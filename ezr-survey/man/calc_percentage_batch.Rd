% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percentage.R
\name{calc_percentage_batch}
\alias{calc_percentage_batch}
\title{Percentages for a batch of questions at once}
\usage{
calc_percentage_batch(
  data = NULL,
  ...,
  by = NULL,
  sort = c("none", "desc", "asc"),
  digits = 0,
  na_rm = TRUE,
  drop = NULL,
  weights = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{...}{Columns to tabulate, using tidyselect (e.g. \code{starts_with("demo_")}
or \verb{demo_gender, demo_edu}).}

\item{by, sort, digits, na_rm, drop, weights}{Passed to \code{\link[=calc_percentage]{calc_percentage()}} (so a
\code{wpct} column appears when weighting is active, and \code{drop} removes unwanted
answers from every question).}
}
\value{
A \link[tibble:tibble]{tibble} with \code{variable}, \code{answer}, \code{n}, \code{pct}
(plus any \code{by} columns).
}
\description{
Runs \code{\link[=calc_percentage]{calc_percentage()}} over several columns and stacks the results into one
tidy table with a \code{variable} column identifying the source question and a
shared \code{answer} column -- handy for tabulating a whole block of questions
(e.g. every \code{demo_} variable) in a single call.
}
\details{
Each selected column is tabulated with \code{\link[=calc_percentage]{calc_percentage()}} and the results are
row-bound, with a \code{variable} column naming the source question and the answers
collected into a shared character \code{answer} column (factor levels differ
between questions, so they are coerced to character when stacked). This is the
tidy long shape you want for faceted plots or a single export of a whole
question block. To send each question to its own Excel tab instead, see
\code{\link[=export_xlsx]{export_xlsx()}}.
}
\examples{
calc_percentage_batch(podracing_survey, demo_gender, demo_job)
#> # A tibble: 8 x 4
#>   variable    answer                n   pct
#>   <chr>       <chr>             <int> <dbl>
#> 1 demo_gender Female              399    42
#> 2 demo_gender Male                525    55
#> # ... and so on for each answer of each variable

calc_percentage_batch(podracing_survey, starts_with("demo_"))
}
\seealso{
\code{\link[=calc_percentage]{calc_percentage()}}, \code{\link[=export_xlsx]{export_xlsx()}}.

Other summaries:
\code{\link[=calc_percentage]{calc_percentage()}},
\code{\link[=calc_percentage_multi]{calc_percentage_multi()}},
\code{\link[=calc_summary]{calc_summary()}},
\code{\link[=crosstab]{crosstab()}}
}
\concept{summaries}
