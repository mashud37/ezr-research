% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comments.R
\name{sample_comments}
\alias{sample_comments}
\title{Pick a random sample of open-text comments}
\usage{
sample_comments(
  data = NULL,
  ...,
  n = 8,
  min_chars = 30,
  max_chars = 400,
  exclude = NULL,
  by_column = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{...}{One or more comment columns, using tidyselect (e.g. \code{nps_com},
\code{show_com}, or \code{dplyr::ends_with("_com")}).}

\item{n}{Number of comments to sample. With \code{by_column = TRUE}, this is per
column.}

\item{min_chars}{Drop comments with this many characters or fewer. Default
\code{30}.}

\item{max_chars}{Truncate longer comments to this length. Default \code{400}.}

\item{exclude}{Optional character vector of (case-insensitive) regex terms;
comments matching any are dropped (e.g. \code{c("friend", "recommend")}).}

\item{by_column}{If \code{TRUE} (default), sample \code{n} from each column; if \code{FALSE},
sample \code{n} from the pooled comments.}
}
\value{
A \link[tibble:tibble]{tibble} with \code{source}, \code{comment} and \code{length}.
}
\description{
Pulls a tidy, presentation-ready sample of comments out of one or more
free-text columns, applying the usual clean-ups: drop blanks and very short
answers, optionally exclude comments matching unwanted terms, and truncate to
a maximum length. The result feeds straight into \code{\link[=plot_quotes_tree]{plot_quotes_tree()}}.
}
\details{
Verbatims need clean-up before they go on a slide: this drops blanks and very
short answers, optionally removes comments matching unwanted terms (\code{exclude}),
and truncates the long ones. With \code{by_column = TRUE} you get \code{n} from each
source column (e.g. an even split across \code{nps_com} and \code{show_com}); with
\code{FALSE}, \code{n} from the pooled set. The result feeds \code{\link[=plot_quotes_tree]{plot_quotes_tree()}}. For a
set that is varied rather than uniformly random, use \code{\link[=sample_comments_diverse]{sample_comments_diverse()}}.
}
\examples{
sample_comments(consumer_survey, nps_com, show_com, n = 3)
}
\seealso{
\code{\link[=sample_comments_diverse]{sample_comments_diverse()}} for a diversity-aware sample,
\code{\link[=plot_quotes_tree]{plot_quotes_tree()}}.

Other comments:
\code{\link[=sample_comments_diverse]{sample_comments_diverse()}}
}
\concept{comments}
