% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_summary.R
\name{export_summary_xlsx}
\alias{export_summary_xlsx}
\title{Export a per-question summary workbook (table + chart per sheet)}
\usage{
export_summary_xlsx(
  data = NULL,
  ...,
  path = NULL,
  by = NULL,
  chart = TRUE,
  sort = c("none", "desc", "asc"),
  digits = 0,
  weights = NULL,
  width = 6,
  height = 3.4
)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}}) is
used.}

\item{...}{Variables to summarise, using tidyselect (e.g.
\code{starts_with("ratings_")} or \verb{demo_gender, satis_return}). One worksheet per
question. If omitted, every question is written: each eligible column plus
any multi-select block, skipping identifier and free-text columns.}

\item{path}{Output \code{.xlsx} path. If \code{NULL} (default), the workbook is written
to \code{ezrsurvey-outputs/summary.xlsx} in the working directory. Missing
directories are created.}

\item{by}{Optional grouping column(s) for the tables (passed to
\code{\link[=calc_percentage]{calc_percentage()}} / \code{\link[=calc_summary]{calc_summary()}} / \code{\link[=calc_percentage_multi]{calc_percentage_multi()}}). The
chart is always the ungrouped distribution.}

\item{chart}{Include the chart beneath each table. Default \code{TRUE}.}

\item{sort}{Level ordering for categorical tables, passed to
\code{\link[=calc_percentage]{calc_percentage()}}: \code{"none"} (default, respecting any registered order),
\code{"desc"} or \code{"asc"}. The chart always auto-lays-out its bars.}

\item{digits}{Decimal places for percentages. Default \code{0}.}

\item{weights}{Survey weighting, passed to the table and chart helpers (see
\code{\link[=calc_percentage]{calc_percentage()}}). \code{NULL} (default) uses the session scheme if set.
Multi-select blocks are always unweighted.}

\item{width, height}{Chart size in inches. Default \verb{6 x 3.4}.}
}
\value{
Invisibly the \code{path} written.
}
\description{
Writes one Excel workbook with a worksheet per question, each holding that
question's summary table and its chart. Numeric questions get a
\code{\link[=calc_summary]{calc_summary()}} table and a histogram; categorical questions get a
\code{\link[=calc_percentage]{calc_percentage()}} table and a \code{\link[=plot_bars]{plot_bars()}} chart; check-all-that-apply
blocks get a \code{\link[=calc_percentage_multi]{calc_percentage_multi()}} table (based on the respondents who
picked any option) and a bar chart. This is the "hand the client a tabbed
workbook" counterpart to the slide/Word builders (\code{\link[=report_deck]{report_deck()}}). Requires
the suggested \code{openxlsx2} package (the only xlsx writer here that can embed
images).
}
\details{
Worksheets are named from each question (cleaned to valid, unique Excel names).
The table is written from cell \code{A1}; the chart, when included, is rendered to a
temporary PNG and embedded a few rows below it. A numeric column (after
\code{\link[=ensure_numeric]{ensure_numeric()}}) is treated as a scale question (summary + histogram);
columns that share a prefix and each hold a single option or blank (e.g.
\verb{motivations_*}) are treated as one check-all-that-apply question; anything
else is categorical (percentages + bar chart). With no \code{...}, every question in
the data is written and identifier / free-text columns are skipped (and named
in a message). For a single table or a pre-named list of tables in one workbook
without charts, see \code{\link[=export_xlsx]{export_xlsx()}} / \code{\link[=save_data]{save_data()}}.
}
\examples{
\donttest{
# named questions
tmp <- tempfile(fileext = ".xlsx")
export_summary_xlsx(podracing_survey, demo_gender, satis_return, nps_value,
                    path = tmp)

# or every question at once (identifier / free-text columns are skipped)
tmp2 <- tempfile(fileext = ".xlsx")
export_summary_xlsx(podracing_survey, path = tmp2)
}
}
\seealso{
\code{\link[=export_xlsx]{export_xlsx()}}, \code{\link[=save_data]{save_data()}}, \code{\link[=report_deck]{report_deck()}},
\code{\link[=calc_percentage_batch]{calc_percentage_batch()}}.

Other save:
\code{\link[=export_xlsx]{export_xlsx()}},
\code{\link[=save_data]{save_data()}},
\code{\link[=save_output]{save_output()}},
\code{\link[=save_plot]{save_plot()}}
}
\concept{save}
