% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_y_pct}
\alias{scale_y_pct}
\title{Continuous y-scale capped at a nice maximum with optional percent labels}
\usage{
scale_y_pct(values = NULL, unit = 25, pad = 0, max = NULL, labels = TRUE, ...)
}
\arguments{
\item{values}{Numeric vector used to determine the axis ceiling, typically
the column being plotted. If \code{NULL} (default) the limit is left to
ggplot2 and only the label formatting is applied.}

\item{unit, pad}{Passed to \code{\link[=nice_max]{nice_max()}}.}

\item{max}{Optional fixed upper limit. If supplied (e.g. \code{100}), it overrides
the dynamic \code{\link[=nice_max]{nice_max()}} ceiling. Defaults to \code{NULL}.}

\item{labels}{One of:
\itemize{
\item \code{TRUE} (default) -- format breaks as \code{"42\%"}.
\item \code{FALSE} -- hide axis labels entirely (\code{labels = NULL}).
\item a function -- used directly as the \code{labels} argument.
}}

\item{...}{Further arguments passed to \code{\link[ggplot2:scale_y_continuous]{ggplot2::scale_y_continuous()}}.}
}
\value{
A ggplot2 scale you can add to a plot with \code{+}.
}
\description{
A thin wrapper around \code{\link[ggplot2:scale_y_continuous]{ggplot2::scale_y_continuous()}} that sets the upper
limit with \code{\link[=nice_max]{nice_max()}} and, by default, formats the axis as percentages.
Designed for the percentage bar charts produced by \code{\link[=calc_percentage]{calc_percentage()}}.
}
\details{
The upper limit is chosen in one of three ways, in order of precedence: a
fixed \code{max} if you supply one; otherwise \code{\link[=nice_max]{nice_max()}} of \code{values}; otherwise
left to ggplot2. Labels default to whole-percent strings via \code{\link[=label_pct]{label_pct()}}.
\code{\link[=plot_bars]{plot_bars()}} calls this for you, reading its \code{unit} and \code{max} from the
\code{pct_axis_unit} / \code{pct_axis_max} options (see \code{\link[=ezrsurvey_options]{ezrsurvey_options()}}), so you
rarely add it by hand -- but it is exported for custom charts.
}
\examples{
df <- calc_percentage(consumer_survey, demo_gender)
library(ggplot2)
ggplot(df, aes(demo_gender, pct)) +
  geom_col() +
  scale_y_pct(df$pct)            # axis capped at a tidy multiple, "\%" labels
}
\seealso{
\code{\link[=nice_max]{nice_max()}}, \code{\link[=label_pct]{label_pct()}}, \code{\link[=plot_bars]{plot_bars()}}.

Other scales:
\code{\link[=label_pct]{label_pct()}},
\code{\link[=nice_max]{nice_max()}}
}
\concept{scales}
