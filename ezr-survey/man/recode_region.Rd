% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region.R
\name{recode_region}
\alias{recode_region}
\alias{recode_subregion}
\title{Look up the region or subregion for a country}
\usage{
recode_region(x, which = c("region", "subregion"), quiet = FALSE)

recode_subregion(x, quiet = FALSE)
}
\arguments{
\item{x}{A character vector of country names.}

\item{which}{\code{"region"} (default) or \code{"subregion"}.}

\item{quiet}{If \code{FALSE} (default), warn about unmatched countries. Set \code{TRUE}
to silence the warning.}
}
\value{
A character vector of regions (or subregions), \code{NA} where unmatched.
}
\description{
Vectorised lookup from country name to world \code{region} or finer \code{subregion},
using the bundled \link{country_region} table. Matching is case-insensitive and
whitespace-tolerant; blanks and non-answers (see \code{\link[=na_blank]{na_blank()}}) become \code{NA},
and unmatched countries become \code{NA} with a one-line warning so spelling
mismatches are easy to spot.
}
\details{
Matching is done on a lower-cased, trimmed country name against the bundled
\link{country_region} table (182 countries), so case and stray whitespace don't
matter. Blanks and non-answers are blanked with \code{\link[=na_blank]{na_blank()}} first, and any
country that doesn't match -- usually a spelling variant the table doesn't
carry -- returns \code{NA} with a warning listing the offenders, so you can spot
and fix them. Set \code{quiet = TRUE} inside pipelines where you have already
checked the coverage. \code{region} is the coarse level (e.g. "Europe");
\code{subregion} is finer (e.g. "Western Europe").
}
\examples{
recode_region(c("Germany", "Japan", "Brazil"))
#> [1] "Europe"        "Asia"          "South America"

recode_subregion(c("Germany", "Japan"))
#> [1] "Western Europe" "East Asia"

# unmatched -> NA (warning suppressed here)
recode_region(c("Germany", "Atlantis"), quiet = TRUE)
#> [1] "Europe" NA
}
\seealso{
\code{\link[=add_region]{add_region()}}, \link{country_region}.

Other recode:
\code{\link[=add_region]{add_region()}},
\code{\link[=bin_numeric]{bin_numeric()}},
\code{\link[=clean_label]{clean_label()}},
\code{\link[=drop_items]{drop_items()}},
\code{\link[=ensure_numeric]{ensure_numeric()}},
\code{\link[=generation_scheme]{generation_scheme()}},
\code{\link[=na_blank]{na_blank()}},
\code{\link[=nps_group]{nps_group()}},
\code{\link[=recode_age]{recode_age()}},
\code{\link[=recode_generation]{recode_generation()}},
\code{\link[=recode_likert]{recode_likert()}}
}
\concept{recode}
