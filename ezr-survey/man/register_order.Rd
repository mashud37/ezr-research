% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orders.R
\name{register_order}
\alias{register_order}
\title{Register a reusable ordinal level order}
\usage{
register_order(name, levels, vars = NULL, prefixes = NULL, overwrite = TRUE)
}
\arguments{
\item{name}{Short name for the order (e.g. \code{"education"}).}

\item{levels}{Character vector of the levels in ascending order.}

\item{vars}{Optional exact column names this order applies to (e.g.
\code{"demo_edu"}).}

\item{prefixes}{Optional column-name prefixes this order applies to (e.g.
\code{"edu_"}). A variable matches if its name starts with any prefix.}

\item{overwrite}{Overwrite an existing order of the same name. Default \code{TRUE}.}
}
\value{
Invisibly the order \code{name}.
}
\description{
Survey scales (education, Likert, frequency, ...) have a natural order that is
tedious to retype as \code{levels = c(...)} every time. Register an order once,
link it to the variable names and/or column prefixes it applies to, and the
tabulating helpers (e.g. \code{\link[=calc_percentage]{calc_percentage()}}) will look it up and apply it
automatically. Persist your orders in a profile with \code{\link[=save_ezrsurvey_profile]{save_ezrsurvey_profile()}}.
}
\details{
An order links a set of \code{levels} to the columns it applies to, by exact name
(\code{vars}) and/or by prefix (\code{prefixes}). Once registered, \code{\link[=calc_percentage]{calc_percentage()}},
\code{\link[=calc_percentage_multi]{calc_percentage_multi()}} and \code{\link[=crosstab]{crosstab()}} look it up automatically (via
\code{\link[=order_for]{order_for()}}) whenever you don't pass an explicit \code{levels}/\code{sort}, so a
question always comes out in the right order without retyping it. Register
interactively for the session, or store orders in a profile with
\code{\link[=save_ezrsurvey_profile]{save_ezrsurvey_profile()}} so they load every session.
}
\examples{
register_order(
  "education",
  levels = c("Less than high school", "High school or equivalent",
             "Some college but no degree", "Associate degree",
             "Bachelor degree", "Masters degree or higher"),
  vars = "demo_edu"
)
calc_percentage(consumer_survey, demo_edu)   # rows now in education order
remove_order("education")
}
\seealso{
\code{\link[=order_for]{order_for()}}, \code{\link[=apply_order]{apply_order()}}, \code{\link[=list_orders]{list_orders()}},
\code{\link[=register_order_presets]{register_order_presets()}}.

Other config:
\code{\link[=apply_order]{apply_order()}},
\code{\link[=edit_ezrsurvey_profile]{edit_ezrsurvey_profile()}},
\code{\link[=ezrsurvey_options]{ezrsurvey_options()}},
\code{\link[=ezrsurvey_profile_paths]{ezrsurvey_profile_paths()}},
\code{\link[=get_dataset]{get_dataset()}},
\code{\link[=get_order]{get_order()}},
\code{\link[=list_orders]{list_orders()}},
\code{\link[=load_ezrsurvey_profile]{load_ezrsurvey_profile()}},
\code{\link[=order_for]{order_for()}},
\code{\link[=register_order_presets]{register_order_presets()}},
\code{\link[=remove_order]{remove_order()}},
\code{\link[=reset_ezrsurvey_options]{reset_ezrsurvey_options()}},
\code{\link[=save_ezrsurvey_profile]{save_ezrsurvey_profile()}},
\code{\link[=use_dataset]{use_dataset()}},
\code{\link[=use_ezrsurvey_profile]{use_ezrsurvey_profile()}}
}
\concept{config}
