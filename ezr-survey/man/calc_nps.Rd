% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{calc_nps}
\alias{calc_nps}
\title{Net Promoter Score}
\usage{
calc_nps(data = NULL, value, by = NULL, weights = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{value}{The 0-10 recommendation column (unquoted).}

\item{by}{Optional grouping column(s); see \code{\link[=calc_percentage]{calc_percentage()}}.}

\item{weights}{Survey weighting: \code{NULL} (default) uses the session scheme from
\code{\link[=set_weights]{set_weights()}} if set; \code{FALSE} forces unweighted; or pass an ad-hoc scheme.
When weighting is active \code{nps} is the weighted score (\code{n} stays the
unweighted base).}
}
\value{
A \link[tibble:tibble]{tibble} with \code{n} (valid responses) and \code{nps} (an
integer from -100 to 100), one row per group when \code{by} is given.
}
\description{
Computes NPS from a 0-10 recommendation question. The score is
\verb{\% promoters - \% detractors}, equivalently \code{100 * mean(nps_group(x))} over the
signed \code{-1/0/1} coding (see \code{\link[=nps_group]{nps_group()}}).
}
\details{
Respondents are bucketed by \code{\link[=nps_group]{nps_group()}} into detractors (0--6), passives
(7--8) and promoters (9--10); the score is the percentage of promoters minus
the percentage of detractors, which equals \code{100 * mean()} of the signed
\code{-1/0/1} coding. Missing or out-of-range scores are dropped before the mean.
Text answers are coerced with \code{\link[=ensure_numeric]{ensure_numeric()}}. Use \code{\link[=plot_nps]{plot_nps()}} for the
full 0--10 distribution chart and \code{\link[=plot_nps_gauge]{plot_nps_gauge()}} for a single-number
gauge.
}
\examples{
calc_nps(podracing_survey, nps_value)
#> # A tibble: 1 x 2
#>       n   nps
#>   <int> <dbl>
#> 1  1000     7

calc_nps(podracing_survey, nps_value, by = region)
}
\seealso{
\code{\link[=nps_group]{nps_group()}}, \code{\link[=plot_nps]{plot_nps()}}, \code{\link[=plot_nps_gauge]{plot_nps_gauge()}}.

Other modelling:
\code{\link[=calc_importance]{calc_importance()}},
\code{\link[=ipm_model]{ipm_model()}}
}
\concept{modelling}
