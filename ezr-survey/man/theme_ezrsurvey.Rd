% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_ezrsurvey}
\alias{theme_ezrsurvey}
\alias{theme_ezrsurvey_x}
\alias{theme_ezrsurvey_y}
\alias{theme_ezrsurvey_xy}
\title{ezrsurvey ggplot2 themes}
\usage{
theme_ezrsurvey(base_size = 11, base_family = "sans", transparent = FALSE)

theme_ezrsurvey_x(base_size = 11, base_family = "sans", transparent = FALSE)

theme_ezrsurvey_y(base_size = 11, base_family = "sans", transparent = FALSE)

theme_ezrsurvey_xy(base_size = 11, base_family = "sans", transparent = FALSE)
}
\arguments{
\item{base_size}{Base font size in points. Defaults to \code{11}.}

\item{base_family}{Base font family. Defaults to \code{"sans"}.}

\item{transparent}{If \code{TRUE}, make the plot, panel and legend backgrounds
transparent (handy for slides). Defaults to \code{FALSE}.}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object to add to a plot with \code{+}.
}
\description{
A clean, presentation-oriented theme family generalised from the original
report themes (\verb{theme_esl*}). The base theme strips chart junk -- no ticks,
no gridlines, centred bold titles -- which suits labelled bar charts that
carry their own data labels. The \verb{_x} / \verb{_y} / \verb{_xy} variants add back faint
major gridlines on the named axes.
}
\details{
A clean, presentation-first look: no axis ticks, no chart junk, bold centred
titles, and faint gridlines only where you ask for them. The base
\code{theme_ezrsurvey()} strips gridlines entirely (best for bar charts that carry
their own data labels); the \verb{_x} / \verb{_y} / \verb{_xy} variants add back faint major
gridlines on the named axes. \code{transparent = TRUE} makes the backgrounds
transparent for slides. All the \verb{plot_*()} helpers apply one of these for you.
}
\examples{
library(ggplot2)
df <- calc_percentage(consumer_survey, demo_gender)
ggplot(df, aes(demo_gender, pct)) + geom_col() + theme_ezrsurvey()
ggplot(df, aes(demo_gender, pct)) + geom_col() + theme_ezrsurvey_y()
}
\seealso{
Other themes:
\code{\link{pal_rating}},
\code{\link[=scale_fill_nps]{scale_fill_nps()}},
\code{\link[=scale_fill_rating]{scale_fill_rating()}},
\code{\link[=theme_transparent]{theme_transparent()}}
}
\concept{themes}
