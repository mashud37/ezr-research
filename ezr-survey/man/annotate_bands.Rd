% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decisions.R
\name{annotate_bands}
\alias{annotate_bands}
\title{Annotate a plot with labelled decision bands}
\usage{
annotate_bands(
  plot,
  bands,
  axis = c("x", "y"),
  at = NULL,
  label_offset = NULL,
  labels = TRUE,
  text_colour = NULL,
  text_size = NULL,
  linewidth = 1
)
}
\arguments{
\item{plot}{A ggplot object.}

\item{bands}{A band specification: a data frame (or one of \code{\link[=bands_rating_3]{bands_rating_3()}},
\code{\link[=bands_rating_5]{bands_rating_5()}}, \code{\link[=bands_nps]{bands_nps()}}) with columns \code{from}, \code{to}, \code{label} and
optionally \code{colour}.}

\item{axis}{Which axis the bands run along: \code{"x"} (default) or \code{"y"}.}

\item{at}{Position on the \emph{opposite} axis at which to draw the marker line and
labels (e.g. the top of a bar chart). If \code{NULL}, inferred from the plot's
panel range.}

\item{label_offset}{Distance to nudge labels away from the marker line, in
data units of the opposite axis. If \code{NULL} (default), 4\% of the opposite
axis range, so labels sit clear of the line instead of on top of it.}

\item{labels}{Whether to draw the band labels. Defaults to \code{TRUE}.}

\item{text_colour, text_size}{Label appearance. \code{text_colour = NULL} (default)
uses each band's own colour; \code{text_size = NULL} (default) matches the
theme's base font size (11 pt) instead of a fixed geom size.}

\item{linewidth}{Marker line width. Defaults to \code{1}.}
}
\value{
The plot with the band layers added.
}
\description{
Programmatically adds the BAD / OK / GOOD style decision markers seen across
the original survey charts: for each band a thin coloured marker line runs
along one axis, with a bold centred label. This turns the copy-pasted
\code{geom_rect()} + \code{annotate("text", x = median(seq(from, to, .1)), ...)} blocks
into one call, so any chart can be given consistent "where's good, where's
bad" guidance.
}
\details{
Bands turn a chart into a decision aid: a thin coloured marker line runs along
one axis for each band, with a bold centred label, so a reader instantly sees
where "good" and "bad" lie. Supply a band spec (a data frame with \code{from},
\code{to}, \code{label} and optional \code{colour}, or one of the \code{\link[=bands_rating_3]{bands_rating_3()}}
presets), the \code{axis} the bands run along, and \code{at} -- the position on the
opposite axis for the marker line (usually the top of the panel; inferred from
the plot when omitted).
}
\examples{
\dontrun{
plot_ipm_base \%>\%
  annotate_bands(bands_rating_3(), axis = "x", at = 30)
}
}
\seealso{
\code{\link[=mark_value]{mark_value()}} to add a single "you are here" marker.

Other decisions:
\code{\link[=bands_rating_3]{bands_rating_3()}},
\code{\link[=mark_value]{mark_value()}}
}
\concept{decisions}
