% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_nps}
\alias{plot_nps}
\title{NPS distribution slide (0-10 scale with group labels)}
\usage{
plot_nps(data = NULL, value, title = NULL)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{value}{The 0-10 recommendation column (unquoted). Text such as
\code{"8 - likely"} is salvaged with \code{\link[=ensure_numeric]{ensure_numeric()}}.}

\item{title}{Optional title; defaults to \code{"Net Promoter Score of: <score>"}.}
}
\value{
A ggplot object.
}
\description{
The canonical Net Promoter Score chart: the full 0-10 recommendation
distribution as labelled bars coloured by NPS group, with the detractor /
passive / promoter shares called out in coloured boxes across the top and the
overall NPS in the title. Reproduces the NPS slide from the original report.
}
\details{
Unlike \code{\link[=plot_nps_gauge]{plot_nps_gauge()}} (which takes a single score), this reads the raw
0--10 column and draws the full distribution: one labelled bar per score,
coloured red/amber/green by NPS group, with the detractor/passive/promoter
shares called out across the top and the overall score in the title. It is the
detailed companion slide to the gauge.
}
\examples{
p <- plot_nps(podracing_survey, nps_value)
# p is a ggplot; print(p) to draw it
}
\seealso{
\code{\link[=calc_nps]{calc_nps()}}, \code{\link[=plot_nps_gauge]{plot_nps_gauge()}}.

Other plots:
\code{\link[=plot_bars]{plot_bars()}},
\code{\link[=plot_ipm]{plot_ipm()}},
\code{\link[=plot_nps_gauge]{plot_nps_gauge()}},
\code{\link[=plot_quotes_tree]{plot_quotes_tree()}},
\code{\link[=plot_stacked_rating]{plot_stacked_rating()}}
}
\concept{plots}
