% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_rating_grid}
\alias{plot_rating_grid}
\title{A stacked rating chart for a whole block of questions}
\usage{
plot_rating_grid(data = NULL, prefix, levels = NULL, digits = 2, ...)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}}).}

\item{prefix}{The block's shared column prefix, e.g. \code{"ratings_"}. It is
stripped from the feature labels.}

\item{levels}{The scale's answer wordings, worst first. \code{NULL} (default)
looks the block up in the order registry (\code{\link[=register_order]{register_order()}}), falling back
to the \code{\link[=recode_likert]{recode_likert()}} default five-point scale.}

\item{digits}{Decimal places kept in the underlying percentages. Default \code{2}.}

\item{...}{Passed to \code{\link[=plot_stacked_rating]{plot_stacked_rating()}} (\code{palette}, \code{label_min},
\code{show_average}).}
}
\value{
A ggplot object.
}
\description{
The one-line form of \code{\link[=plot_stacked_rating]{plot_stacked_rating()}} for the common case: a block of
columns sharing a prefix, each asking the same rating question about a
different feature or brand. Tabulates the block, tidies the question names,
numbers the answers by their position on the scale and stacks the result.
}
\details{
Answers are numbered by their position in \code{levels}, which is what orders the
features by their weighted mean and colours the segments worst-to-best. A
wording that is not on the scale cannot be numbered, so it is reported rather
than being silently dropped into an unlabelled segment: check it against the
raw export, since exports often differ from a questionnaire by a typo.
Registering the block once with
\code{register_order("likeability", levels, prefixes = "partner_likeability_")}
removes the \code{levels} argument from every later call.
}
\examples{
plot_rating_grid(podracing_survey, "ratings_")

plot_rating_grid(podracing_survey, "partner_likeability_",
                 levels = c("Very unlikeable", "Unlikeable",
                            "Likeable", "Very likeable"))
}
\seealso{
\code{\link[=plot_stacked_rating]{plot_stacked_rating()}}, \code{\link[=calc_percentage_batch]{calc_percentage_batch()}},
\code{\link[=register_order]{register_order()}}.

Other plots:
\code{\link[=plot_bars]{plot_bars()}},
\code{\link[=plot_gauges]{plot_gauges()}},
\code{\link[=plot_ipm]{plot_ipm()}},
\code{\link[=plot_nps]{plot_nps()}},
\code{\link[=plot_nps_gauge]{plot_nps_gauge()}},
\code{\link[=plot_quotes_tree]{plot_quotes_tree()}},
\code{\link[=plot_stacked_rating]{plot_stacked_rating()}}
}
\concept{plots}
