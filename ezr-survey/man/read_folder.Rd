% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{read_folder}
\alias{read_folder}
\title{Read and stack every CSV in a folder}
\usage{
read_folder(
  path,
  pattern = "\\\\.csv$",
  id = "file",
  all_character = TRUE,
  locale = readr::locale(encoding = "UTF-8"),
  ...
)
}
\arguments{
\item{path}{Directory to read from.}

\item{pattern}{Regular expression matching the files to load. Defaults to
\code{"\\\\.csv$"} (all CSVs).}

\item{id}{Name of the column in which to record each row's source filename.
Defaults to \code{"file"}. Set to \code{NULL} to omit it.}

\item{all_character}{If \code{TRUE} (default), every column is read as character.
This keeps ragged survey exports stackable; recode types afterwards with
the \verb{recode_*} helpers. If \code{FALSE}, types are guessed per file.}

\item{locale}{A \code{\link[readr:locale]{readr::locale()}} controlling encoding etc. Defaults to a
UTF-8 locale; pass \code{readr::locale(encoding = "windows-1252")} for legacy
exports.}

\item{...}{Further arguments passed to \code{\link[readr:read_csv]{readr::read_csv()}}.}
}
\value{
A single \link[tibble:tibble]{tibble} of all files stacked together. If
columns differ across files, missing columns are filled with \code{NA}.
}
\description{
Loads all CSV files matching \code{pattern} in \code{path}, reading each as character
(so survey codes never get silently coerced), tags every row with its source
filename, and row-binds the lot into one tibble. Generalises the
\code{list.files() \%>\% map(read.csv) \%>\% bind_rows()} opening of the original
scripts.
}
\details{
Every matching file is read and the results are row-bound, so a folder of
monthly or per-event exports becomes one dataset. By default every column is
read as text (\code{all_character = TRUE}); this is deliberate, because survey
exports are ragged (a column that is numeric in one file may carry "Prefer
not to answer" in another), and stacking text columns never fails. Recode the
types you need afterwards with \verb{recode_*()} / \code{\link[=ensure_numeric]{ensure_numeric()}}. The \code{id}
column records which file each row came from -- split it into metadata with
\code{\link[=parse_filename]{parse_filename()}}. For legacy encodings pass a \code{locale}, e.g.
\code{readr::locale(encoding = "windows-1252")}.
}
\examples{
# build a tiny folder of CSVs, then read them back
dir <- tempfile(); dir.create(dir)
readr::write_csv(head(podracing_survey, 3), file.path(dir, "a.csv"))
readr::write_csv(head(podracing_survey, 2), file.path(dir, "b.csv"))
read_folder(dir)[, c("file", "respondent_id")]
}
\seealso{
\code{\link[=select_prefix]{select_prefix()}}, \code{\link[=parse_filename]{parse_filename()}}.

Other import:
\code{\link[=parse_filename]{parse_filename()}},
\code{\link[=select_prefix]{select_prefix()}},
\code{\link[=select_suffix]{select_suffix()}}
}
\concept{import}
