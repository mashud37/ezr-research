% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosstab.R
\name{crosstab}
\alias{crosstab}
\title{Cross-tabulate two survey questions}
\usage{
crosstab(
  data = NULL,
  x,
  y,
  cell = c("count", "row_pct", "col_pct", "total_pct"),
  value = NULL,
  fn = mean,
  wide = TRUE,
  digits = NULL,
  na_rm = TRUE,
  drop = NULL,
  weights = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{Row variable (unquoted).}

\item{y}{Column variable (unquoted).}

\item{cell}{What to put in each cell when \code{value} is not given: \code{"count"}
(default), \code{"row_pct"}, \code{"col_pct"} or \code{"total_pct"}.}

\item{value}{Optional numeric column (unquoted) to aggregate per cell instead
of counting.}

\item{fn}{Aggregation function used with \code{value}. Default \code{\link[=mean]{mean()}}.}

\item{wide}{If \code{TRUE} (default), return one row per \code{x} with a column per \code{y}
level; if \code{FALSE}, return the tidy long form.}

\item{digits}{Decimal places for percentage / aggregated cells. Default \code{0}
for percentages, \code{2} when \code{value} is supplied.}

\item{na_rm}{Drop blanks / non-answers in \code{x}, \code{y} (and \code{NA} in \code{value}).
Default \code{TRUE}.}

\item{drop}{Optional character vector of answer values to remove from both \code{x}
and \code{y} before tabulating (e.g. \code{c("Other")}), matched case-insensitively.
Defaults to the \code{drop_answers} option. See \code{\link[=drop_items]{drop_items()}}.}

\item{weights}{Survey weighting: \code{NULL} (default) uses the session scheme from
\code{\link[=set_weights]{set_weights()}} if set; \code{FALSE} forces unweighted; or pass an ad-hoc scheme.
When weighting is active the cells default to weighted values -- weighted
counts, weighted percentages, or (for a numeric \code{value}) the weighted mean.}
}
\value{
A \link[tibble:tibble]{tibble}: wide (x plus one column per y level) or
long (\code{x}, \code{y}, \code{value}).
}
\description{
Builds a crosstab from two categorical columns: \code{x} forms the rows and \code{y} the
columns. The cell contents are chosen with \code{cell} -- counts or row/column/total
percentages -- or, when a numeric \code{value} column is supplied, an aggregate of
that column (e.g. the mean spend) for each \code{x} by \code{y} combination. Registered
orders (see \code{\link[=register_order]{register_order()}}) are applied to \code{x} and \code{y} automatically.
}
\details{
Choose what the cells mean with \code{cell}: \code{"row_pct"} makes each \strong{row} sum to
100 (the distribution of \code{y} within each \code{x}), \code{"col_pct"} makes each
\strong{column} sum to 100, \code{"total_pct"} is the share of the whole table, and
\code{"count"} is the raw frequency. Supplying a numeric \code{value} switches the cells
to an aggregate of that column -- by default the mean -- which answers
questions like "what is the average NPS for each region by gender?". Blanks
and non-answers in \code{x}/\code{y} are dropped when \code{na_rm = TRUE}, and any registered
orders (\code{\link[=register_order]{register_order()}}) set the row/column ordering automatically.
}
\examples{
# counts
crosstab(podracing_survey, demo_gender, region)

# row percentages: gender split within each region (each row ~ 100)
crosstab(podracing_survey, region, demo_gender, cell = "row_pct")

# mean NPS for each region x gender cell
crosstab(podracing_survey, region, demo_gender, value = nps_value, fn = mean)
}
\seealso{
\code{\link[=calc_percentage]{calc_percentage()}}, \code{\link[=compare_values]{compare_values()}}, \code{\link[=register_order]{register_order()}}.

Other summaries:
\code{\link[=calc_percentage]{calc_percentage()}},
\code{\link[=calc_percentage_batch]{calc_percentage_batch()}},
\code{\link[=calc_percentage_multi]{calc_percentage_multi()}},
\code{\link[=calc_summary]{calc_summary()}}
}
\concept{summaries}
