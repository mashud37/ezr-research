% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-coerce.R
\name{ensure_numeric}
\alias{ensure_numeric}
\title{Coerce text to numeric, salvaging embedded numbers}
\usage{
ensure_numeric(x, name = NULL, quiet = FALSE)
}
\arguments{
\item{x}{A vector. Numeric input is returned as-is.}

\item{name}{Optional column/argument name, used only to make the message
clearer.}

\item{quiet}{If \code{FALSE} (default), emit a one-line message when coercion
happens (and note any values that could not be parsed). Set \code{TRUE} to
silence it.}
}
\value{
A numeric vector the same length as \code{x}.
}
\description{
A defensive helper for the common survey-export headache where a column that
should be numeric arrives as text -- \code{"25 years"}, \code{"8 - very likely"},
\code{"3.5/5"}. It returns \code{x} unchanged if it is already numeric; otherwise it
pulls the first number out of each value (via a digit pattern) and coerces to
numeric, leaving genuinely unparseable entries as \code{NA}. The ezrsurvey
functions that need numbers (e.g. \code{\link[=calc_nps]{calc_nps()}}, \code{\link[=calc_summary]{calc_summary()}}) call this
for you, so they "just work" on lightly messy data.
}
\details{
The first numeric token in each value is extracted with the pattern
\verb{-?[0-9]*\\.?[0-9]+}, which captures negatives and decimals but takes only the
\emph{first} number it finds -- so \code{"8 - very likely"} becomes \code{8}, not \code{-8}. A
value that is genuinely numeric already is returned untouched (no copy, no
message). When \code{quiet = FALSE} a single informative message reports that
coercion happened and how many values could not be parsed; the internal
callers (\code{\link[=calc_nps]{calc_nps()}}, \code{\link[=calc_summary]{calc_summary()}}, \code{\link[=ipm_model]{ipm_model()}}, \code{\link[=bin_numeric]{bin_numeric()}}, ...)
pass \code{quiet = TRUE} so they don't spam your console. Truly blank entries
(\code{""}/\code{NA}) are not counted as parse failures.
}
\examples{
ensure_numeric(c("25 years", "31", "forty"), quiet = TRUE)
#> [1] 25 31 NA

ensure_numeric(c("8 - very likely", "10", "3.5/5"), quiet = TRUE)
#> [1]  8.0 10.0  3.5

ensure_numeric(c(1, 2, 3))    # already numeric, returned as-is
#> [1] 1 2 3
}
\seealso{
\code{\link[=recode_age]{recode_age()}}, \code{\link[=recode_likert]{recode_likert()}}, \code{\link[=na_blank]{na_blank()}}.

Other recode:
\code{\link[=add_region]{add_region()}},
\code{\link[=bin_numeric]{bin_numeric()}},
\code{\link[=drop_items]{drop_items()}},
\code{\link[=generation_scheme]{generation_scheme()}},
\code{\link[=na_blank]{na_blank()}},
\code{\link[=nps_group]{nps_group()}},
\code{\link[=recode_age]{recode_age()}},
\code{\link[=recode_generation]{recode_generation()}},
\code{\link[=recode_likert]{recode_likert()}},
\code{\link[=recode_region]{recode_region()}}
}
\concept{recode}
