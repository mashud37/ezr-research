% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generation.R
\name{recode_generation}
\alias{recode_generation}
\title{Recode age or birth year into a generational cohort}
\usage{
recode_generation(x, input = c("age", "year"), year = NULL, scheme = NULL)
}
\arguments{
\item{x}{A numeric (or coercible) vector of ages or birth years.}

\item{input}{What \code{x} represents: \code{"age"} (default) or \code{"year"} (birth year).}

\item{year}{Reference year for the age-to-birth-year conversion. If \code{NULL}
(default), uses the \code{current_year} option (see \code{\link[=ezrsurvey_options]{ezrsurvey_options()}}) or the
system year.}

\item{scheme}{Either a scheme name passed to \code{\link[=generation_scheme]{generation_scheme()}} (default
from the \code{generation_scheme} option) or your own data frame with \code{from} and
\code{label} columns.}
}
\value{
A character vector of cohort labels; values outside the scheme's range
become \code{NA}.
}
\description{
Maps either an \strong{age} (converted to a birth year using a reference year) or a
\strong{birth year} directly onto a generational cohort such as Millennial or
Gen Z. Text input like \code{"34 years"} is salvaged with \code{\link[=ensure_numeric]{ensure_numeric()}}.
}
\details{
When \code{input = "age"}, the birth year is \code{year - age}, where \code{year} defaults to
the \code{current_year} option or the system year -- so set
\code{ezrsurvey_options(current_year = 2026)} to keep results stable across runs.
When \code{input = "year"}, \code{x} is treated as the birth year directly. The birth
year is then bucketed into the scheme's cohorts (see \code{\link[=generation_scheme]{generation_scheme()}});
birth years before the earliest cohort return \code{NA}. Use this for
generational reporting; for simple age bands use \code{\link[=recode_age]{recode_age()}}.
}
\examples{
recode_generation(c(1990, 2001, 1968), input = "year")
#> [1] "Millennial" "Gen Z"      "Gen X"

# from age, with an explicit reference year
recode_generation(c(36, 25), input = "age", year = 2026)
#> [1] "Millennial" "Gen Z"
}
\seealso{
\code{\link[=generation_scheme]{generation_scheme()}}, \code{\link[=recode_age]{recode_age()}}, \code{\link[=ezrsurvey_options]{ezrsurvey_options()}}.

Other recode:
\code{\link[=add_region]{add_region()}},
\code{\link[=bin_numeric]{bin_numeric()}},
\code{\link[=clean_label]{clean_label()}},
\code{\link[=drop_items]{drop_items()}},
\code{\link[=ensure_numeric]{ensure_numeric()}},
\code{\link[=generation_scheme]{generation_scheme()}},
\code{\link[=na_blank]{na_blank()}},
\code{\link[=nps_group]{nps_group()}},
\code{\link[=recode_age]{recode_age()}},
\code{\link[=recode_likert]{recode_likert()}},
\code{\link[=recode_region]{recode_region()}}
}
\concept{recode}
