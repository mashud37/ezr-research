% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand.R
\name{use_brand}
\alias{use_brand}
\title{Adopt an organisation brand from a PowerPoint or Word template}
\usage{
use_brand(template = NULL, colors = NULL, fonts = NULL, quiet = FALSE)
}
\arguments{
\item{template}{Path to a \code{.pptx} or \code{.docx} file. Its OOXML theme (accent
colours, heading/body typefaces) is extracted, and the file is registered
as the default reference document for reports of the matching format.
\code{NULL} to set colours/fonts directly without a template.}

\item{colors}{Optional character vector of hex colours overriding the
extracted accents. A single colour sets only the primary; a vector sets
the full accent palette (first entry = primary). Invalid entries are
dropped with a warning.}

\item{fonts}{Optional character vector overriding the extracted typefaces:
either a single body font, or \code{c(major = "...", minor = "...")}.}

\item{quiet}{If \code{TRUE}, suppress the confirmation message.}
}
\value{
Invisibly a \code{ezrsurvey_brand} list (see \code{\link[=brand_info]{brand_info()}}).
}
\description{
Reads the colour and font theme out of your organisation's \code{.pptx} / \code{.docx}
template and makes it the session default for everything ezrsurvey produces:
charts pick up the brand accent colours and body font, and the template file
itself becomes the default reference document for \code{\link[=report_new]{report_new()}},
\code{\link[=report_deck]{report_deck()}} and \code{\link[=scaffold_report]{scaffold_report()}}. One call, and analysis output is
on-brand.
}
\details{
Extraction reads \code{theme1.xml} inside the template (requires the suggested
\code{xml2} package): accent colours 1-6 become \code{brand_colors} (first accent =
\code{brand_color_primary}, the default fill of \code{\link[=plot_bars]{plot_bars()}}), and the minor
(body) typeface becomes the default \code{base_family} of \code{\link[=theme_ezrsurvey]{theme_ezrsurvey()}} --
but only when that font is actually installed on this machine, so charts
never render with substituted glyph boxes. Set
\code{ezrsurvey_options(brand_fonts_enabled = FALSE)} to keep brand colours but
ignore brand fonts.

Everything lands in ordinary \code{\link[=ezrsurvey_options]{ezrsurvey_options()}} (\verb{brand_*} keys), so you
can equally set the values by hand or persist them in a YAML profile; a
later \code{use_brand()} call simply overwrites them. Semantic palettes
(\link{pal_rating}, \link{pal_nps}) deliberately keep their meaning-carrying colours
and are not rebranded. Use \code{\link[=clear_brand]{clear_brand()}} to return to the neutral look.
}
\examples{
\dontshow{if (requireNamespace("xml2", quietly = TRUE) && requireNamespace("officer", quietly = TRUE)) withAutoprint(\{ # examplesIf}
tmp <- tempfile(fileext = ".pptx")
print(officer::read_pptx(), target = tmp)
use_brand(tmp, quiet = TRUE)
brand_info()
clear_brand()
\dontshow{\}) # examplesIf}
# Or set brand values directly, no template needed:
use_brand(colors = c("#0B5394", "#E69138"), fonts = "Georgia", quiet = TRUE)
clear_brand()
}
\seealso{
\code{\link[=brand_info]{brand_info()}}, \code{\link[=clear_brand]{clear_brand()}}, \code{\link[=pal_brand]{pal_brand()}},
\code{\link[=scale_fill_brand]{scale_fill_brand()}}, \code{\link[=report_new]{report_new()}}, \code{\link[=scaffold_report]{scaffold_report()}}.

Other brand:
\code{\link[=brand_info]{brand_info()}},
\code{\link[=clear_brand]{clear_brand()}},
\code{\link[=pal_brand]{pal_brand()}},
\code{\link[=scale_fill_brand]{scale_fill_brand()}}
}
\concept{brand}
