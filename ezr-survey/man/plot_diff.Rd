% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{plot_diff}
\alias{plot_diff}
\title{Diverging bar chart of differences}
\usage{
plot_diff(
  data,
  label = feature,
  difference = difference,
  limits = NULL,
  digits = 2,
  pos_colour = "#A7C23D",
  neg_colour = "#FFCB3E",
  title = NULL
)
}
\arguments{
\item{data}{A data frame with a label column and a difference column.}

\item{label}{Category/label column (unquoted). Default \code{feature}.}

\item{difference}{Difference column (unquoted). Default \code{difference}.}

\item{limits}{Numeric length-2 y-axis limits. If \code{NULL} (default), a symmetric
range around zero is chosen from the data.}

\item{digits}{Decimal places for the value labels. Default \code{2}.}

\item{pos_colour, neg_colour}{Bar colours for positive / negative differences.}

\item{title}{Optional plot title.}
}
\value{
A ggplot object.
}
\description{
Plots a column of differences (e.g. from \code{\link[=compare_values]{compare_values()}}) as a sorted,
diverging horizontal bar chart: positives in green, negatives in amber, signed
value labels, and a zero reference line. This is the standard "what improved,
what slipped" change chart.
}
\details{
Bars are sorted by the difference and drawn horizontally (largest gain at the
top), coloured green for positive and amber for negative changes, with signed
value labels (\code{+0.20} / \code{-0.30}) nudged to sit just outside each bar and a
zero reference line. When \code{limits} is \code{NULL} the y-axis is made symmetric
around zero from the data so gains and losses are visually comparable; pass
\code{limits = c(-1, 1)} (say) to fix it. Feed it the output of \code{\link[=compare_values]{compare_values()}},
or any data frame with a label column and a numeric difference column.
}
\examples{
a <- data.frame(feature = c("price", "quality", "service"),
                performance = c(3.1, 4.2, 3.5))
b <- data.frame(feature = c("price", "quality", "service"),
                performance = c(2.8, 4.4, 3.9))
p <- compare_values(b, a) |> plot_diff()
# p is a ggplot; print(p) to draw it
}
\seealso{
\code{\link[=compare_values]{compare_values()}}.

Other compare:
\code{\link[=compare_values]{compare_values()}}
}
\concept{compare}
