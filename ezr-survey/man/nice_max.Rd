% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{nice_max}
\alias{nice_max}
\title{Round an axis maximum up to a "nice" value}
\usage{
nice_max(x, unit = 25, pad = 0)
}
\arguments{
\item{x}{A numeric vector. \code{NA} values are ignored.}

\item{unit}{Size of the rounding step (e.g. \code{25} for percentages, \code{5} for
smaller counts). Must be a positive number.}

\item{pad}{Extra headroom added to the result, in the same units as \code{x}.
Useful to leave space for annotations above the tallest bar. Defaults to
\code{0}.}
}
\value{
A single numeric value: the padded, rounded-up ceiling. Returns \code{NA}
if \code{x} is empty or all \code{NA}.
}
\description{
Picks the smallest multiple of \code{unit} that is greater than or equal to
\code{max(x)}. This reproduces the dynamic y-axis trick used throughout the
original survey reports, where bar charts are given a tidy ceiling in units
of 25 (percentages) or 5 (NPS counts) so that data labels never collide with
the top of the panel.
}
\details{
The ceiling is \code{(floor(max(x) / unit) + 1) * unit + pad}: the \emph{next}
multiple of \code{unit} strictly above \code{max(x)}, always leaving headroom even
when the max already sits exactly on a multiple (e.g. \code{nice_max(75)} is
\code{100}, not \code{75}). Use \code{pad} for additional headroom on top of that, e.g.
for annotations above the tallest bar. An all-\code{NA} logical column (a common
shape for an empty survey field) is tolerated and returns \code{NA}. This is the
engine behind \code{\link[=scale_y_pct]{scale_y_pct()}} and the percentage plot wrappers, which is
why their bars never quite touch the top of the panel.
}
\examples{
nice_max(c(12, 63, 40))
#> [1] 75

nice_max(80, unit = 25)        # already need >75, so jumps to 100
#> [1] 100

nice_max(75, unit = 25)        # exact multiple still advances, for headroom
#> [1] 100

nice_max(c(8, 17), unit = 5, pad = 10)   # next multiple of 5 (20) + 10
#> [1] 30
}
\seealso{
\code{\link[=scale_y_pct]{scale_y_pct()}}, \code{\link[=label_pct]{label_pct()}}.

Other scales:
\code{\link[=label_pct]{label_pct()}},
\code{\link[=scale_y_pct]{scale_y_pct()}}
}
\concept{scales}
