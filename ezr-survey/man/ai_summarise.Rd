% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ai.R
\name{ai_summarise}
\alias{ai_summarise}
\title{Summarise a table with a commercial LLM}
\usage{
ai_summarise(
  data,
  instructions = NULL,
  template = "key_findings",
  provider = "openai",
  model = NULL,
  chat = NULL,
  title = NULL,
  context = NULL,
  max_rows = 50,
  ...
)
}
\arguments{
\item{data}{A data frame / tibble to summarise.}

\item{instructions}{Optional extra, free-text instructions appended to the
template's default instruction.}

\item{template}{Prompt template name (see \code{\link[=list_prompts]{list_prompts()}}). Defaults to
\code{"key_findings"}.}

\item{provider, model}{Passed to \code{\link[=ai_chat]{ai_chat()}} when \code{chat} is not supplied.}

\item{chat}{An existing \code{\link[=ai_chat]{ai_chat()}} object to reuse. If \code{NULL} (default), a
fresh chat is created using the template's system prompt.}

\item{title}{Optional context title for the table.}

\item{context}{Optional survey context from \code{\link[=ai_context]{ai_context()}} (or a character
vector), included in the prompt so the model can respect sample sizes and
margins of error.}

\item{max_rows}{Maximum rows of \code{data} to include in the prompt. Defaults to
\code{50}.}

\item{...}{Passed to \code{\link[=ai_chat]{ai_chat()}}.}
}
\value{
The model's response as a length-1 character string.
}
\description{
Sends a summary data frame (e.g. the output of \code{\link[=calc_percentage]{calc_percentage()}},
\code{\link[=diagnose]{diagnose()}} or \code{\link[=ipm_model]{ipm_model()}}) to an LLM together with a named prompt template
and returns the model's text. Only the table is sent -- format your data down
to the figures you want summarised first.
}
\details{
Only the summary table you pass is sent to the provider -- so summarise or
aggregate your data down to the figures you want described first, never the
raw respondent rows. The named \code{template} supplies a system prompt and a
default instruction (see \code{\link[=list_prompts]{list_prompts()}}); add free-text \code{instructions} to
steer a single call. A fresh chat is created per call unless you pass one via
\code{chat}. Requires the suggested \code{ellmer} package and a stored key (see
\code{\link[=set_llm_key]{set_llm_key()}}).
}
\examples{
\dontrun{
calc_percentage(podracing_survey, demo_gender, sort = "desc") \%>\%
  ai_summarise(template = "exec_summary", provider = "openai")
}
}
\seealso{
\code{\link[=ai_report_sections]{ai_report_sections()}} to summarise many tables at once.

Other ai:
\code{\link[=ai_chat]{ai_chat()}},
\code{\link[=ai_context]{ai_context()}},
\code{\link[=ai_report_sections]{ai_report_sections()}},
\code{\link[=delete_llm_key]{delete_llm_key()}},
\code{\link[=get_llm_key]{get_llm_key()}},
\code{\link[=get_prompt]{get_prompt()}},
\code{\link[=has_llm_key]{has_llm_key()}},
\code{\link[=list_llm_keys]{list_llm_keys()}},
\code{\link[=list_prompts]{list_prompts()}},
\code{\link[=register_prompt]{register_prompt()}},
\code{\link[=set_llm_key]{set_llm_key()}}
}
\concept{ai}
