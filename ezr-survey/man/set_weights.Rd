% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{set_weights}
\alias{set_weights}
\title{Set a survey weighting scheme for the session}
\usage{
set_weights(...)
}
\arguments{
\item{...}{One scheme per variable. Two forms are accepted:
\itemize{
\item \strong{vector form} (one argument per variable) carrying a \code{variable} entry and
a share per category:
\code{c(variable = "demo_gender", Male = 0.49, Female = 0.50, "Non-binary" = 0.01)};
\item \strong{named form}: \code{demo_gender = c(Male = 0.49, Female = 0.50)}, where the
argument name is the column.
Shares need not sum to 1 -- they are normalised per variable (so raw
percentages or counts work too).
}}
}
\value{
Invisibly, the parsed scheme (a named list of normalised targets).
}
\description{
Defines target shares for one or more categorical variables (e.g. a known
population split by gender or region). Once set, the summary helpers
(\code{\link[=calc_percentage]{calc_percentage()}}, \code{\link[=calc_nps]{calc_nps()}}, \code{\link[=calc_summary]{calc_summary()}}, \code{\link[=crosstab]{crosstab()}}) weight
their results automatically, so the weighted margins match your targets. Pass
\code{weights = FALSE} to any of them to opt out for a single call.
}
\details{
With a single variable the weights are exact post-stratification:
\code{target_share / sample_share} for each category. With several variables the
weights are found by raking (iterative proportional fitting) so every variable's
weighted margin matches its targets. Weights are normalised to mean 1 (so the
weighted base equals the sample size), and respondents whose weighting value is
blank / a non-answer (see \code{\link[=na_blank]{na_blank()}}) are left unadjusted. A category that
appears in the data but is missing from your targets is an error -- give every
observed category a share. If a default dataset is set, this prints the Kish
design effect and the effective sample size so you can see the precision cost.
}
\examples{
set_weights(c(variable = "demo_gender",
              "As a man" = 0.49, "As a woman" = 0.50,
              "Non-binary person" = 0.01))
calc_percentage(consumer_survey, satis_return)   # gains a wpct column
clear_weights()
}
\seealso{
\code{\link[=clear_weights]{clear_weights()}}, \code{\link[=get_weights]{get_weights()}}, \code{\link[=weight_vector]{weight_vector()}},
\code{\link[=calc_percentage]{calc_percentage()}}.

Other weighting:
\code{\link[=get_weights]{get_weights()}},
\code{\link[=weight_vector]{weight_vector()}}
}
\concept{weighting}
