% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recode.R
\name{bin_numeric}
\alias{bin_numeric}
\title{Bin a numeric vector into labelled groups}
\usage{
bin_numeric(x, breaks, labels, right = FALSE)
}
\arguments{
\item{x}{A numeric vector.}

\item{breaks}{Numeric vector of cut points. With \code{n} labels you need \code{n + 1}
breaks. Use \code{Inf} for an open-ended top band.}

\item{labels}{Character vector of group labels, length \code{length(breaks) - 1}.}

\item{right}{If \code{TRUE}, intervals are closed on the right; if \code{FALSE}
(default) closed on the left. Left-closed matches "18 to 21" style bands.}
}
\value{
A character vector of group labels (values outside the range or \code{NA}
become \code{NA}).
}
\description{
A thin, survey-friendly wrapper around \code{\link[base:cut]{base::cut()}} that returns a character
vector (not a factor) and uses left-closed, right-open intervals by default
so that age bands like 18-21 behave intuitively. Generalises the
\code{case_when(age \%in\% seq(...))} age-grouping pattern from the original
reports.
}
\details{
Bands are built with \code{\link[base:cut]{base::cut()}} using \code{include.lowest = TRUE}, so the very
lowest break is included. With \code{right = FALSE} (the default) a band runs from
its lower break up to \emph{but not including} the next -- i.e. \verb{[18, 25)} -- which
is what you want for age groups like "18 to 24". Text input is salvaged with
\code{\link[=ensure_numeric]{ensure_numeric()}}, so \code{"27 years"} bins correctly. The result is a plain
character vector (not a factor); apply an order later with \code{\link[=register_order]{register_order()}}
or pass \verb{levels =} to \code{\link[=calc_percentage]{calc_percentage()}} if you need a specific display
order.
}
\examples{
bin_numeric(c(15, 19, 27, 41),
            breaks = c(0, 18, 25, 35, Inf),
            labels = c("<18", "18-24", "25-34", "35+"))
#> [1] "<18"   "18-24" "25-34" "35+"
}
\seealso{
\code{\link[=recode_age]{recode_age()}} for a ready-made age-band wrapper, \code{\link[=ensure_numeric]{ensure_numeric()}}.

Other recode:
\code{\link[=add_region]{add_region()}},
\code{\link[=ensure_numeric]{ensure_numeric()}},
\code{\link[=generation_scheme]{generation_scheme()}},
\code{\link[=na_blank]{na_blank()}},
\code{\link[=nps_group]{nps_group()}},
\code{\link[=recode_age]{recode_age()}},
\code{\link[=recode_generation]{recode_generation()}},
\code{\link[=recode_likert]{recode_likert()}},
\code{\link[=recode_region]{recode_region()}}
}
\concept{recode}
