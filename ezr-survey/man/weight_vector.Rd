% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{weight_vector}
\alias{weight_vector}
\title{Compute the per-respondent survey weights}
\usage{
weight_vector(data = NULL, weights = NULL)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}}) is
used.}

\item{weights}{A weighting scheme (any form accepted by \code{\link[=set_weights]{set_weights()}}). If
\code{NULL} (default), the session scheme is used.}
}
\value{
A numeric vector of length \code{nrow(data)}, normalised to mean 1.
}
\description{
Returns the weight vector a scheme implies for a dataset -- the same numbers the
summary helpers use internally -- so you can inspect them, attach them as a
column, or feed them to another tool.
}
\details{
This is the function behind the automatic weighting: each respondent's weight is
\code{target_share / sample_share} for their category (raked across variables when
there are several), normalised so the weighted base equals the unweighted one.
Pair it with \code{\link[=set_weights]{set_weights()}}'s reported design effect to judge how much
precision the weighting costs.
}
\examples{
w <- weight_vector(podracing_survey,
                   c(variable = "demo_gender",
                     "Male" = 0.49, "Female" = 0.50,
                     "Non-binary" = 0.01))
round(range(w), 2)
}
\seealso{
\code{\link[=set_weights]{set_weights()}}, \code{\link[=calc_percentage]{calc_percentage()}}.

Other weighting:
\code{\link[=clear_weights_cache]{clear_weights_cache()}},
\code{\link[=get_weights]{get_weights()}},
\code{\link[=set_weights]{set_weights()}}
}
\concept{weighting}
