% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_quotes_tree}
\alias{plot_quotes_tree}
\title{Treemap of selected quotes}
\usage{
plot_quotes_tree(data, label = comment, area = length, colour = "black")
}
\arguments{
\item{data}{A data frame of quotes, e.g. from \code{\link[=sample_comments]{sample_comments()}}.}

\item{label}{Text column (unquoted). Defaults to \code{comment}.}

\item{area}{Tile-size column (unquoted). Defaults to \code{length}.}

\item{colour}{Tile border colour. Defaults to \code{"black"}.}
}
\value{
A ggplot object.
}
\description{
A treemap where each tile is a comment sized by length -- the quote slides
from the original report. Requires the suggested \code{treemapify} package.
}
\details{
Lays selected verbatims out as a treemap, each tile sized by comment length,
so a slide can show real customer voice at a glance. Pair it with
\code{\link[=sample_comments]{sample_comments()}} or \code{\link[=sample_comments_diverse]{sample_comments_diverse()}}, which produce the
\code{comment}/\code{length} columns it expects. Requires the suggested \code{treemapify}
package.
}
\examples{
\dontshow{if (requireNamespace("treemapify", quietly = TRUE)) withAutoprint(\{ # examplesIf}
sample_comments(podracing_survey, nps_com, show_com, n = 5) \%>\%
  plot_quotes_tree()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=sample_comments]{sample_comments()}}, \code{\link[=sample_comments_diverse]{sample_comments_diverse()}}.

Other plots:
\code{\link[=plot_bars]{plot_bars()}},
\code{\link[=plot_ipm]{plot_ipm()}},
\code{\link[=plot_nps]{plot_nps()}},
\code{\link[=plot_nps_gauge]{plot_nps_gauge()}},
\code{\link[=plot_stacked_rating]{plot_stacked_rating()}}
}
\concept{plots}
