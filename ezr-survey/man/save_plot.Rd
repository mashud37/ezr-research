% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{save_plot}
\alias{save_plot}
\title{Quick-save a plot to PNG, SVG or PDF}
\usage{
save_plot(
  plot,
  path = NULL,
  width = 8,
  height = 4.5,
  dpi = 300,
  bg = "transparent",
  ...
)
}
\arguments{
\item{plot}{A ggplot object.}

\item{path}{Output path; the extension sets the format (\code{.png}, \code{.svg},
\code{.pdf}, \code{.jpg}/\code{.jpeg}, \code{.tiff}). If \code{NULL} (default), the plot is written
to \code{outputs/plot.png} in the working directory. Missing directories are
created.}

\item{width, height}{Size in inches. Default \verb{8 x 4.5}.}

\item{dpi}{Raster resolution for PNG/JPG/TIFF. Default \code{300}.}

\item{bg}{Background fill. Default \code{"transparent"} (matches the ezrsurvey
themes); use \code{"white"} for a solid background.}

\item{...}{Passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
The \code{plot}, invisibly.
}
\description{
A thin, pipe-friendly wrapper around \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} that picks the device
from the file extension and uses presentation-friendly defaults (transparent
background, generous size). Returns the plot invisibly, so it drops into a
pipeline without breaking it.
}
\details{
The device is chosen from the file extension, and the plot is returned
invisibly so the call slots into a pipeline without breaking it
(\code{p \%>\% save_plot("p.png") \%>\% print()}). Defaults suit slides: a transparent
background (matching the ezrsurvey themes) and a generous size; pass
\code{bg = "white"} for a solid background. SVG output needs the suggested
\code{svglite} package.
}
\examples{
p <- plot_bars(calc_percentage(podracing_survey, demo_gender))
tmp <- tempfile(fileext = ".png")
save_plot(p, tmp)
file.exists(tmp)
}
\seealso{
\code{\link[=save_data]{save_data()}}, \code{\link[=save_output]{save_output()}}.

Other save:
\code{\link[=export_xlsx]{export_xlsx()}},
\code{\link[=save_data]{save_data()}},
\code{\link[=save_output]{save_output()}}
}
\concept{save}
