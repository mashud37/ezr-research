% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/currency.R
\name{convert_currency}
\alias{convert_currency}
\title{Convert amounts between currencies}
\usage{
convert_currency(amount, from, to = "USD", rates = NULL)
}
\arguments{
\item{amount}{Numeric amounts (text like \code{"$1,200"} is salvaged with
\code{\link[=ensure_numeric]{ensure_numeric()}}).}

\item{from}{Source currency code(s), e.g. \code{"EUR"} (case-insensitive). Common
aliases resolve to their ISO code (e.g. \code{"RMB"} -> \code{"CNY"}).}

\item{to}{Target currency code(s). Defaults to \code{"USD"}.}

\item{rates}{Exchange rates: \code{NULL} (default) for the bundled snapshot, a
named numeric vector of units-per-USD (e.g. \code{c(EUR = 0.92, GBP = 0.79)}),
or a data frame with \code{currency} and \code{per_usd} columns.}
}
\value{
A numeric vector of converted amounts; unknown currency codes yield
\code{NA} with a warning.
}
\description{
Converts monetary amounts from one currency to another using a table of
exchange rates. By default it uses the bundled \link{currency_rates} snapshot
(units per US dollar); pass your own \code{rates} for up-to-date or custom figures.
Because rates share the USD base, any pair is converted as a cross-rate
through USD automatically (e.g. EUR to CNY goes EUR -> USD -> CNY in one call).
\code{amount}, \code{from} and \code{to} are vectorised and recycled, so you can convert a
whole column with per-row source currencies at once.
}
\details{
Each rate is stored as units of the currency per 1 US dollar, so a conversion
is \code{amount / rate(from) * rate(to)} -- which means any pair is handled as a
cross-rate through USD without you doing anything. The bundled
\link{currency_rates} table is a dated, approximate snapshot meant for convenience,
not live trading; for accuracy pass your own \code{rates} (a named vector or a
\code{currency}/\code{per_usd} data frame). Amounts are coerced with \code{\link[=ensure_numeric]{ensure_numeric()}},
codes are upper-cased and resolved through a small alias map (\code{"RMB"} ->
\code{"CNY"}, \code{"EURO"} -> \code{"EUR"}, ...), and any unknown code yields \code{NA} with a
warning rather than a silent wrong number.
}
\examples{
convert_currency(100, "EUR", "USD")
#> [1] 108.7

convert_currency(c(100, 50), from = c("EUR", "GBP"), to = "USD")

convert_currency(100, "EUR", "CNY")          # cross-rate via USD
convert_currency(100, "EUR", "RMB")          # alias -> CNY, same result
}
\seealso{
\code{\link[=add_currency]{add_currency()}}, \code{\link[=list_currencies]{list_currencies()}}, \link{currency_rates}.

Other currency:
\code{\link[=add_currency]{add_currency()}},
\code{\link[=list_currencies]{list_currencies()}}
}
\concept{currency}
