% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recode.R
\name{na_blank}
\alias{na_blank}
\title{Convert blank and boilerplate non-answers to \code{NA}}
\usage{
na_blank(x, also = ezrsurvey_default("na_answers"), trim = TRUE)
}
\arguments{
\item{x}{A character (or factor) vector.}

\item{also}{Additional exact strings to treat as missing. Defaults to the
\code{na_answers} option (\code{"Prefer not to answer"}; see \code{\link[=ezrsurvey_options]{ezrsurvey_options()}}).
Set to \code{character(0)} to only blank out \code{""}.}

\item{trim}{Whether to trim surrounding whitespace before comparing.
Defaults to \code{TRUE}.}
}
\value{
A character vector the same length as \code{x}, with blanks and
non-answers replaced by \code{NA}.
}
\description{
Survey exports are littered with empty strings and standard "non-answer"
options such as "Prefer not to answer". This helper turns those into real
\code{NA} values so they drop out of counts and summaries, replacing the
repetitive \code{filter(x != "", x != "Prefer not to answer")} idiom.
}
\details{
The input is coerced to character (so factors are handled), optionally
whitespace-trimmed, and any value equal to \code{""} or to one of \code{also} becomes
\code{NA}. The default \code{also} reads the \code{na_answers} option, so you can change what
counts as a non-answer package-wide with
\code{ezrsurvey_options(na_answers = ...)}. Most of the \verb{calc_*} helpers call
\code{na_blank()} internally when \code{na_rm = TRUE}, so you usually get this clean-up
for free; call it directly when you want to blank a column in a pipeline.
}
\examples{
na_blank(c("Yes", "", "Prefer not to answer", "No"))
#> [1] "Yes" NA    NA    "No"

# add your own non-answers
na_blank(c("a", "n/a", "N/A"), also = c("n/a", "N/A"))
#> [1] "a" NA  NA
}
\seealso{
\code{\link[=ensure_numeric]{ensure_numeric()}}, \code{\link[=ezrsurvey_options]{ezrsurvey_options()}}.

Other recode:
\code{\link[=add_region]{add_region()}},
\code{\link[=bin_numeric]{bin_numeric()}},
\code{\link[=drop_items]{drop_items()}},
\code{\link[=ensure_numeric]{ensure_numeric()}},
\code{\link[=generation_scheme]{generation_scheme()}},
\code{\link[=nps_group]{nps_group()}},
\code{\link[=recode_age]{recode_age()}},
\code{\link[=recode_generation]{recode_generation()}},
\code{\link[=recode_likert]{recode_likert()}},
\code{\link[=recode_region]{recode_region()}}
}
\concept{recode}
