% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{save_data}
\alias{save_data}
\title{Quick-save a table to CSV, TSV or XLSX}
\usage{
save_data(data, path = NULL, na = "", ...)
}
\arguments{
\item{data}{A data frame / tibble, or (for \code{.xlsx}) a named list of them.}

\item{path}{Output path; the extension sets the format (\code{.csv}, \code{.tsv},
\code{.xlsx}). If \code{NULL} (default), the table is written to \code{outputs/data.csv}
in the working directory. Missing directories are created.}

\item{na}{String to write for missing values. Default \code{""}.}

\item{...}{Passed to the underlying writer (\code{\link[readr:write_csv]{readr::write_csv()}} /
\code{\link[readr:write_tsv]{readr::write_tsv()}} / \code{\link[writexl:write_xlsx]{writexl::write_xlsx()}}).}
}
\value{
\code{data}, invisibly.
}
\description{
A pipe-friendly one-liner for dropping a summary table to disk. The format is
taken from the file extension. For \code{.xlsx}, pass a single data frame for one
sheet, or a \emph{named} list of data frames to write one sheet per element.
Returns its input invisibly so it can sit mid-pipeline.
}
\details{
The format follows the file extension: \code{.csv}/\code{.tsv} via \pkg{readr}, \code{.xlsx}
via the suggested \code{writexl} package. For an Excel workbook, pass one data
frame for a single sheet, or a \emph{named} list of data frames for one sheet each.
Returns its input invisibly so it can sit mid-pipeline. To send several
separate calls to their own tabs in one line, see \code{\link[=export_xlsx]{export_xlsx()}}.
}
\examples{
tab <- calc_percentage(podracing_survey, demo_gender)
tmp <- tempfile(fileext = ".csv")
save_data(tab, tmp)
file.exists(tmp)
}
\seealso{
\code{\link[=save_plot]{save_plot()}}, \code{\link[=save_output]{save_output()}}, \code{\link[=export_xlsx]{export_xlsx()}}.

Other save:
\code{\link[=export_xlsx]{export_xlsx()}},
\code{\link[=save_output]{save_output()}},
\code{\link[=save_plot]{save_plot()}}
}
\concept{save}
