% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{ipm_model}
\alias{ipm_model}
\title{Build an importance / performance model}
\usage{
ipm_model(
  data = NULL,
  outcome,
  rating_prefix,
  recode = TRUE,
  likert_levels = c("Very bad", "Bad", "Ok", "Good", "Very good")
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome}{The outcome column (unquoted), e.g. \code{nps_value}.}

\item{rating_prefix}{Column-name prefix identifying the rating block, e.g.
\code{"ratings_"}.}

\item{recode}{If \code{TRUE} (default), character rating columns are mapped to
1-5 with \code{\link[=recode_likert]{recode_likert()}}; numeric columns are left as-is.}

\item{likert_levels}{Scale wordings passed to \code{\link[=recode_likert]{recode_likert()}} when
recoding.}
}
\value{
A \link[tibble:tibble]{tibble} with \code{feature}, \code{importance},
\code{performance} and \code{perf_class} (the performance band as a 1-5 factor, from
its integer part so a 3.57 mean sits in band 3, used for colouring).
}
\description{
Combines driver \strong{importance} (relative weights against \code{outcome}) with
feature \strong{performance} (mean rating) into the tidy table consumed by
\code{\link[=plot_ipm]{plot_ipm()}}. Worded rating columns are recoded to 1-5 automatically.
}
\details{
An importance/performance model answers two questions per feature at once:
\emph{how much does it drive the outcome} (importance, via \code{\link[=calc_importance]{calc_importance()}}) and
\emph{how well are we doing on it} (performance, the mean 1--5 rating). Plotting one
against the other (\code{\link[=plot_ipm]{plot_ipm()}}) reveals priorities: high-importance,
low-performance features are where to invest. Worded rating columns are mapped
to 1--5 with \code{\link[=recode_likert]{recode_likert()}} automatically (\code{recode = TRUE}); the prefix is
stripped from feature names; and \code{perf_class} buckets performance into a 1--5
factor by its integer part (a 3.57 mean is band 3, not 4) for colouring.
Requires the suggested \code{rwa} package.
}
\examples{
\dontshow{if (requireNamespace("rwa", quietly = TRUE)) withAutoprint(\{ # examplesIf}
ipm_model(podracing_survey, nps_value, "ratings_")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=calc_importance]{calc_importance()}}, \code{\link[=plot_ipm]{plot_ipm()}}, \code{\link[=compare_values]{compare_values()}}.

Other modelling:
\code{\link[=calc_importance]{calc_importance()}},
\code{\link[=calc_nps]{calc_nps()}}
}
\concept{modelling}
