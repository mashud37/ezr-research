% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percentage.R
\name{calc_percentage}
\alias{calc_percentage}
\title{Count a categorical question as percentages}
\usage{
calc_percentage(
  data = NULL,
  column,
  by = NULL,
  sort = c("none", "desc", "asc"),
  levels = NULL,
  digits = 0,
  wide = FALSE,
  na_rm = TRUE
)
}
\arguments{
\item{data}{A data frame. If omitted, the session default set with
\code{\link[=use_dataset]{use_dataset()}} is used.}

\item{column}{The categorical column to tabulate (unquoted).}

\item{by}{Optional grouping column(s). Percentages are computed \emph{within} each
group so they sum to ~100 per group. Pass one bare name (\code{by = year}) or
several with \code{c()} (\code{by = c(year, game)}).}

\item{sort}{Level ordering of \code{column}: \code{"none"} (default, data order),
\code{"desc"} (largest percentage first) or \code{"asc"}.}

\item{levels}{Optional character vector giving an explicit level order for
\code{column} (the "custom" ordering mode). Overrides \code{sort}. If omitted and
\code{sort = "none"}, a registered order for this variable is applied
automatically (see \code{\link[=register_order]{register_order()}}).}

\item{digits}{Decimal places for the percentage. Defaults to \code{0}.}

\item{wide}{If \code{TRUE}, pivot to one row per \code{by} group and one column per
answer (dropping \code{n}), matching the wide summary tables in the original
workflow. Defaults to \code{FALSE} (tidy long form).}

\item{na_rm}{If \code{TRUE} (default), blanks and "Prefer not to answer" responses
(see \code{\link[=na_blank]{na_blank()}}) are dropped before counting.}
}
\value{
A \link[tibble:tibble]{tibble} with \code{column}, \code{n} and \code{pct} (long form),
or one row per group with an answer column each (wide form).
}
\description{
The headline helper: replaces the ubiquitous
\code{count(x) |> mutate(pct = round(n / sum(n) * 100)) |> select(-n)} dance with a
single call. Returns both the raw counts and the percentages, optionally
grouped by one or more variables and optionally pivoted to a wide
cross-tabulation.
}
\details{
Percentages are computed \emph{within} each group, so they sum to about 100 per
group (subject to rounding). The level order of \code{column} is decided in this
order of precedence: an explicit \code{levels} argument; then a non-\code{"none"}
\code{sort}; then a registered order for the variable (see \code{\link[=register_order]{register_order()}});
otherwise data order. When \code{by} is omitted, the \code{default_by} option is used if
set (see \code{\link[=ezrsurvey_options]{ezrsurvey_options()}}), so you can apply a standard breakdown without
repeating it. \code{wide = TRUE} pivots to one row per group with a column per
answer -- the shape you want for a slide table or an Excel tab.
}
\examples{
calc_percentage(consumer_survey, demo_gender)
#> # A tibble: 3 x 3
#>   demo_gender           n   pct
#>   <chr>             <int> <dbl>
#> 1 As a man            648    70
#> 2 As a woman          228    24
#> 3 Non-binary person    56     6

# largest first
calc_percentage(consumer_survey, demo_gender, sort = "desc")

# grouped and pivoted to a wide cross-tab
calc_percentage(consumer_survey, satis_return, by = region, wide = TRUE)
}
\seealso{
\code{\link[=calc_percentage_multi]{calc_percentage_multi()}} for check-all-that-apply questions,
\code{\link[=calc_percentage_batch]{calc_percentage_batch()}} for many questions at once, \code{\link[=calc_summary]{calc_summary()}} for
numeric variables, \code{\link[=register_order]{register_order()}} for reusable level orders.

Other summaries:
\code{\link[=calc_percentage_batch]{calc_percentage_batch()}},
\code{\link[=calc_percentage_multi]{calc_percentage_multi()}},
\code{\link[=calc_summary]{calc_summary()}},
\code{\link[=crosstab]{crosstab()}}
}
\concept{summaries}
