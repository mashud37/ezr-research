% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnose}
\alias{diagnose}
\title{Survey precision diagnostics for one or more columns}
\usage{
diagnose(
  data = NULL,
  ...,
  type = c("auto", "mean", "prop"),
  by = NULL,
  z = 1.96,
  digits = 2
)
}
\arguments{
\item{data}{A data frame.}

\item{...}{Columns to diagnose, using tidyselect (e.g.
\code{starts_with("ratings_")}, \code{demo_gender}).}

\item{type}{Force the estimate type: \code{"auto"} (default, detect per column),
\code{"mean"} or \code{"prop"}.}

\item{by}{Optional grouping column(s); diagnostics are computed within group.}

\item{z}{Margin-of-error multiplier passed to \code{\link[=margin_of_error]{margin_of_error()}}. Defaults
to \code{1.96} (95\%).}

\item{digits}{Decimal places for the numeric output columns. Defaults to \code{2}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per column (per group), with
columns \code{variable}, \code{type}, \code{unit}, \code{n}, \code{estimate}, \code{se}, \code{rse}, \code{moe} and
\code{precision}. For \code{type == "mean"}, \code{unit} is \code{"points"}; for proportions it
is \code{"ppt"} (percentage points) and \code{estimate} is a percentage.
}
\description{
The headline diagnostics wrapper: for each selected column it auto-detects
whether it is a point estimate (numeric, e.g. a 1-5 rating) or a proportion
(categorical), and returns a tidy table of sample size, estimate, standard
error, relative standard error and margin of error -- i.e. the whole appendix
"Data info" block as one call.
}
\details{
For categorical columns the reported estimate is the \strong{largest answer
category's} share, which gives a concrete \verb{+/-} percentage-point margin to
quote in a report footnote.

Each column is classified (or forced via \code{type}) as a point estimate or a
proportion. For numeric columns the estimate is the mean and the error is in
rating points; for categorical columns the estimate is the \strong{largest answer
category's} share, which gives a single concrete \verb{+/-} percentage-point
margin to quote (the largest category is also the worst case among the
observed categories, so it is a sensible headline). The \code{precision} column is
the five-band rating from \code{\link[=rse_rating]{rse_rating()}}. This is the per-variable form of the
report-appendix "Data info" block; for the narrative bullet-point version see
\code{\link[=precision_summary]{precision_summary()}}.
}
\examples{
diagnose(podracing_survey, demo_gender, nps_value)
#> # A tibble: 2 x 9
#>   variable    type  unit      n estimate    se   rse   moe precision
#>   <chr>       <chr> <chr> <int>    <dbl> <dbl> <dbl> <dbl> <chr>
#> 1 demo_gender prop  ppt     951     55.2  1.61  2.92  3.16 high precision
#> 2 nps_value   mean  points 1000      7.56 0.06  0.74  0.11 high precision

diagnose(podracing_survey, starts_with("ratings_"))
}
\seealso{
\code{\link[=precision_summary]{precision_summary()}}, \code{\link[=rse_rating]{rse_rating()}}, \code{\link[=se_mean]{se_mean()}}, \code{\link[=se_prop]{se_prop()}}.

Other diagnostics:
\code{\link[=margin_of_error]{margin_of_error()}},
\code{\link[=precision_summary]{precision_summary()}},
\code{\link[=rse]{rse()}},
\code{\link[=rse_rating]{rse_rating()}},
\code{\link[=se_mean]{se_mean()}},
\code{\link[=se_prop]{se_prop()}}
}
\concept{diagnostics}
