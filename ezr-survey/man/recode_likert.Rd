% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recode.R
\name{recode_likert}
\alias{recode_likert}
\title{Recode a worded rating scale to integers}
\usage{
recode_likert(
  x,
  levels = c("Very bad", "Bad", "Ok", "Good", "Very good"),
  synonyms = NULL
)
}
\arguments{
\item{x}{A character (or factor) vector of worded answers.}

\item{levels}{Character vector of the scale's answer wordings in ascending
order. The first element maps to \code{1}, the last to \code{length(levels)}.
Defaults to a 5-point bad-to-good scale.}

\item{synonyms}{Optional named list mapping a canonical level (one of
\code{levels}) to a character vector of alternative spellings that should map to
the same integer. Matching is by case-insensitive substring.}
}
\value{
An integer vector the same length as \code{x}; unmatched values become
\code{NA}.
}
\description{
Maps the worded answers of an ordinal rating question (e.g. "Very bad" ...
"Very good") onto integers \code{1:length(levels)}. Matching is case-insensitive
and tolerant of common synonyms supplied via \code{synonyms}, reproducing the
\verb{case_when(str_detect("Very bad") ...)} recoding blocks from the reports.
}
\details{
Matching happens in three passes, in order: (1) an exact, case-insensitive,
trimmed match against \code{levels}; (2) a substring fallback, so prefixed exports
like \code{"4 - Good"} still resolve; (3) any \code{synonyms} you supply. This makes the
function robust to the small wording differences between survey tools (e.g.
"Dissatisfied" vs "Bad"). Turning ratings into integers is the first step of
\code{\link[=ipm_model]{ipm_model()}} and lets you average a scale with \code{\link[=calc_summary]{calc_summary()}}.
}
\examples{
recode_likert(c("Very bad", "Ok", "Good", "Very good"))
#> [1] 1 3 4 5

# substring fallback copes with numbered labels
recode_likert("4 - Good")
#> [1] 4

# map another tool's wording onto the same scale
recode_likert(c("Dissatisfied", "Satisfied"),
              synonyms = list(Bad = "Dissatisfied", Good = "Satisfied"),
              levels = c("Very bad", "Bad", "Ok", "Good", "Very good"))
#> [1] 2 4
}
\seealso{
\code{\link[=nps_group]{nps_group()}}, \code{\link[=ipm_model]{ipm_model()}}, \code{\link[=calc_summary]{calc_summary()}}.

Other recode:
\code{\link[=add_region]{add_region()}},
\code{\link[=bin_numeric]{bin_numeric()}},
\code{\link[=ensure_numeric]{ensure_numeric()}},
\code{\link[=generation_scheme]{generation_scheme()}},
\code{\link[=na_blank]{na_blank()}},
\code{\link[=nps_group]{nps_group()}},
\code{\link[=recode_age]{recode_age()}},
\code{\link[=recode_generation]{recode_generation()}},
\code{\link[=recode_region]{recode_region()}}
}
\concept{recode}
