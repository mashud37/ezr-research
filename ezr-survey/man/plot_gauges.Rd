% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_gauges}
\alias{plot_gauges}
\title{Stacked score gauges (NPS over average quality, etc.)}
\usage{
plot_gauges(
  scores,
  scales = NULL,
  title = NULL,
  height = 0.5,
  label_size = NULL
)
}
\arguments{
\item{scores}{A \strong{named} numeric vector; each name labels a gauge and each
value is placed on its scale. The first is drawn at the top.}

\item{scales}{Which band scale each gauge uses: \code{"nps"} (a -100..100 Net
Promoter scale) or \code{"rating"} (a 1..5 quality scale). Length 1 (recycled)
or one per score. \code{NULL} (default) infers \code{"rating"} for a value in 1..5
and \code{"nps"} otherwise.}

\item{title}{Optional title. \code{NULL} (default) draws none, so a slide title can
carry the wording instead.}

\item{height}{Bar thickness in plot units (row spacing is 1). Defaults to
\code{0.5}, keeping each bar deliberately thin so several share the panel.}

\item{label_size}{Band-label text size. \code{NULL} (default) is a compact
\code{10 / .pt}.}
}
\value{
A ggplot object.
}
\description{
Draws two or more scores as thin banded gauge bars stacked one above another,
each on its own scale with a "you are here" marker. The headline summary
slide: the Net Promoter Score on top and the average feature quality rating
below it, so both key numbers sit together instead of one fat bar filling the
slide.
}
\details{
Each gauge is normalised to its own scale, so an NPS of \code{+23} and a quality
rating of \code{3.4} line up on a shared 0..1 panel with the band boundaries drawn
where each scale puts them (the NPS gauge reuses the \code{\link[=plot_nps_gauge]{plot_nps_gauge()}} bands;
the rating gauge uses \code{\link[=bands_rating_3]{bands_rating_3()}}). The score for each gauge is printed
beside its label, and a black marker shows where it lands. Because the bars
are thin and stacked, this is the summary-slide companion to the detailed
\code{\link[=plot_nps]{plot_nps()}} distribution and the \code{\link[=plot_ipm]{plot_ipm()}} driver matrix.
}
\examples{
nps <- calc_nps(podracing_survey, nps_value)$nps
quality <- 3.4
p <- plot_gauges(c("Net Promoter Score" = nps,
                   "Average quality rating" = quality))
}
\seealso{
\code{\link[=plot_nps_gauge]{plot_nps_gauge()}}, \code{\link[=calc_nps]{calc_nps()}}, \code{\link[=ipm_model]{ipm_model()}}.

Other plots:
\code{\link[=plot_bars]{plot_bars()}},
\code{\link[=plot_ipm]{plot_ipm()}},
\code{\link[=plot_nps]{plot_nps()}},
\code{\link[=plot_nps_gauge]{plot_nps_gauge()}},
\code{\link[=plot_quotes_tree]{plot_quotes_tree()}},
\code{\link[=plot_rating_grid]{plot_rating_grid()}},
\code{\link[=plot_stacked_rating]{plot_stacked_rating()}}
}
\concept{plots}
