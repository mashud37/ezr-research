% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_xlsx}
\alias{export_xlsx}
\title{Export several tables to one Excel workbook, a tab each}
\usage{
export_xlsx(..., path = NULL, sheet_names = NULL)
}
\arguments{
\item{...}{Data frames to write, one per tab. Name them to set tab names
(e.g. \code{gender = calc_percentage(d, demo_gender)}).}

\item{path}{Output \code{.xlsx} path. If \code{NULL} (default), the workbook is
written to \code{outputs/tables.xlsx} in the working directory. Missing
directories are created.}

\item{sheet_names}{Optional character vector of tab names (overrides argument
names).}
}
\value{
Invisibly \code{path}.
}
\description{
A quick way to drop a handful of summaries into a single \code{.xlsx}, one per
worksheet -- e.g. \code{export_xlsx(calc_percentage(d, a), calc_percentage(d, b), path = "out.xlsx")}. Tabs are named from the argument names you give, else
from each table's question/first column, else \code{Sheet1}, \code{Sheet2}, ...
}
\details{
Each argument becomes one worksheet. Tab names are taken from the argument
names you give (\code{gender = ...}), otherwise from each table's \code{variable} column
or first column, otherwise \code{Sheet1}, \code{Sheet2}, ...; they are then cleaned to
valid, unique Excel names (<= 31 characters, with \verb{[ ] : * ? / \\\\} stripped).
This is the quick way to drop a set of summaries into one workbook for a
colleague. Requires the suggested \code{writexl} package.

[ ]: R:\%20
}
\examples{
tmp <- tempfile(fileext = ".xlsx")
export_xlsx(
  gender = calc_percentage(podracing_survey, demo_gender),
  calc_percentage(podracing_survey, demo_job),
  path = tmp
)
file.exists(tmp)
}
\seealso{
\code{\link[=save_data]{save_data()}} for a single table or a pre-named list.

Other save:
\code{\link[=save_data]{save_data()}},
\code{\link[=save_output]{save_output()}},
\code{\link[=save_plot]{save_plot()}}
}
\concept{save}
