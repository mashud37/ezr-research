% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prompts.R
\name{ai_context}
\alias{ai_context}
\title{Describe the survey behind a table for AI summaries}
\usage{
ai_context(
  data = NULL,
  n = NULL,
  question = NULL,
  base = NULL,
  precision = NULL,
  fieldwork = NULL,
  notes = NULL
)
}
\arguments{
\item{data}{Optional raw survey data (one row per respondent). Used only to
derive \code{n} and, via \code{\link[=precision_summary]{precision_summary()}}, the precision notes; the rows
themselves are never sent to a provider.}

\item{n}{Sample size. Derived from \code{data} when omitted.}

\item{question}{The verbatim question wording behind the table(s).}

\item{base}{Who was asked, e.g. \code{"All respondents"} or
\code{"Detractors only (n = 142)"}.}

\item{precision}{Character vector of precision notes. Derived from \code{data}
via \code{\link[=precision_summary]{precision_summary()}} when omitted and \code{data} is given.}

\item{fieldwork}{Fieldwork description, e.g. \code{"12-19 May 2026, online panel"}.}

\item{notes}{Any further context worth giving the model.}
}
\value{
An \code{ezrsurvey_ai_context} list.
}
\description{
Bundles the survey facts a good analyst would want next to any table --
sample size, question wording, base, precision -- so \code{\link[=ai_summarise]{ai_summarise()}} and
\code{\link[=report_deck]{report_deck()}} can pass them to the model. With the raw survey \code{data}, the
sample size and precision bullets are derived for you.
}
\details{
Context is what turns a generic summary into an analyst's summary: with a
sample size and margin of error present, the built-in templates flag
differences within the margin of error instead of narrating noise, and cite
the base alongside findings. Reuse one context object across every
\code{\link[=ai_summarise]{ai_summarise()}} call and \code{\link[=report_deck]{report_deck()}} build for the same survey.
}
\examples{
ctx <- ai_context(
  podracing_survey,
  question = "How likely are you to recommend pod racing?",
  fieldwork = "Simulated data"
)
ctx
}
\seealso{
\code{\link[=ai_summarise]{ai_summarise()}}, \code{\link[=ai_report_sections]{ai_report_sections()}}, \code{\link[=precision_summary]{precision_summary()}}.

Other ai:
\code{\link[=ai_chat]{ai_chat()}},
\code{\link[=ai_report_sections]{ai_report_sections()}},
\code{\link[=ai_summarise]{ai_summarise()}},
\code{\link[=delete_llm_key]{delete_llm_key()}},
\code{\link[=get_llm_key]{get_llm_key()}},
\code{\link[=get_prompt]{get_prompt()}},
\code{\link[=has_llm_key]{has_llm_key()}},
\code{\link[=list_llm_keys]{list_llm_keys()}},
\code{\link[=list_prompts]{list_prompts()}},
\code{\link[=register_prompt]{register_prompt()}},
\code{\link[=set_llm_key]{set_llm_key()}}
}
\concept{ai}
