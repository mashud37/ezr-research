% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{use_dataset}
\alias{use_dataset}
\title{Set a default dataset for the session}
\usage{
use_dataset(data)
}
\arguments{
\item{data}{A data frame to use as the default.}
}
\value{
\code{data}, invisibly (so it can sit in a pipe).
}
\description{
Registers a data frame as the session's default, so the analysis helpers can
be called without repeating \code{data} every time. Set it once at the top of a
script or Quarto document and then omit \code{data} (or pass it by pipe).
}
\details{
With a default set you can drop \code{data} entirely and name the column
positionally, the way you would in the tidyverse:
\itemize{
\item \code{calc_percentage(demo_gender)} -- data taken from the default;
\item \code{podracing_survey \%>\% calc_percentage(demo_gender)} -- explicit pipe;
\item \code{calc_percentage(other_survey, demo_gender)} -- an explicit data frame
always wins over the default.
}

The helpers tell a column from a data frame by looking at the first argument:
a data frame is used as the data; anything else (a bare column name) is taken
as the first column and the data comes from the default. Helpers that select
several columns through \code{...} (e.g. \code{\link[=diagnose]{diagnose()}}, \code{\link[=calc_percentage_batch]{calc_percentage_batch()}})
work the same way -- \code{diagnose(starts_with("ratings_"))} needs no \code{data}.

The default applies to the analysis helpers that read a raw survey
(\code{calc_percentage()}, \code{calc_summary()}, \code{calc_nps()}, \code{diagnose()},
\code{crosstab()}, \code{ipm_model()}, \code{sample_comments()}, ...). It does \strong{not} apply
to plot/report helpers that consume an already-summarised table. The setting
lives only in the current R session; clear it with \code{\link[=clear_dataset]{clear_dataset()}}.
}
\examples{
use_dataset(podracing_survey)
calc_percentage(demo_gender)   # data taken from the default
calc_nps(nps_value)
clear_dataset()
}
\seealso{
\code{\link[=get_dataset]{get_dataset()}}, \code{\link[=clear_dataset]{clear_dataset()}}, \code{\link[=has_dataset]{has_dataset()}}.

Other config:
\code{\link[=apply_order]{apply_order()}},
\code{\link[=edit_ezrsurvey_profile]{edit_ezrsurvey_profile()}},
\code{\link[=ezrsurvey_options]{ezrsurvey_options()}},
\code{\link[=ezrsurvey_profile_paths]{ezrsurvey_profile_paths()}},
\code{\link[=get_dataset]{get_dataset()}},
\code{\link[=get_order]{get_order()}},
\code{\link[=list_orders]{list_orders()}},
\code{\link[=load_ezrsurvey_profile]{load_ezrsurvey_profile()}},
\code{\link[=order_for]{order_for()}},
\code{\link[=register_order]{register_order()}},
\code{\link[=register_order_presets]{register_order_presets()}},
\code{\link[=remove_order]{remove_order()}},
\code{\link[=reset_ezrsurvey_options]{reset_ezrsurvey_options()}},
\code{\link[=save_ezrsurvey_profile]{save_ezrsurvey_profile()}},
\code{\link[=use_ezrsurvey_profile]{use_ezrsurvey_profile()}}
}
\concept{config}
