% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{select_suffix}
\alias{select_suffix}
\title{Select identifier and suffixed columns}
\usage{
select_suffix(data = NULL, suffix, keep = NULL)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{suffix}{One or more column-name suffixes to keep (e.g. \code{"_com"}, or
\code{c("_com", "_score")}).}

\item{keep}{Optional character vector of additional column names to retain in
front of the suffixed block (e.g. an id column).}
}
\value{
A data frame with \code{keep} columns followed by all suffix-matching
columns.
}
\description{
The mirror of \code{\link[=select_prefix]{select_prefix()}}: keeps a handful of id columns plus every
column sharing a suffix -- the \code{select(id, ends_with("_com"))} idiom -- in
one tidy call.
}
\details{
Some questionnaire blocks are marked by a trailing tag rather than a leading
one (\verb{_com} open-text follow-ups, \verb{_score} derived columns), so this grabs
one such block plus a couple of id columns without naming every variable.
Order is \code{keep} first, then the suffix-matching columns in their original
order.
}
\examples{
select_suffix(podracing_survey, "_com", keep = "respondent_id")
}
\seealso{
\code{\link[=select_prefix]{select_prefix()}}, \code{\link[=read_folder]{read_folder()}}.

Other import:
\code{\link[=parse_filename]{parse_filename()}},
\code{\link[=read_folder]{read_folder()}},
\code{\link[=select_prefix]{select_prefix()}}
}
\concept{import}
