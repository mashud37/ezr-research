% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{select_prefix}
\alias{select_prefix}
\title{Select identifier and prefixed columns}
\usage{
select_prefix(data = NULL, prefix, keep = NULL)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{prefix}{One or more column-name prefixes to keep (e.g. \code{"demo_"}, or
\code{c("demo_", "ratings_")}).}

\item{keep}{Optional character vector of additional column names to retain in
front of the prefixed block (e.g. an id column).}
}
\value{
A data frame with \code{keep} columns followed by all prefix-matching
columns.
}
\description{
Keeps a handful of id columns plus every column sharing a prefix -- the
\code{select(id, starts_with("demo_"))} idiom -- in one tidy call.
}
\details{
Survey questionnaires are usually organised by prefix (\code{demo_}, \code{ratings_},
\code{partner_}, ...), so this is a quick way to grab one block plus a couple of id
columns without naming every variable. Order is \code{keep} first, then the
prefix-matching columns in their original order.
}
\examples{
select_prefix(consumer_survey, "demo_", keep = "respondent_id")
select_prefix(consumer_survey, c("ratings_", "partner_"))
}
\seealso{
\code{\link[=read_folder]{read_folder()}}, \code{\link[=calc_percentage_multi]{calc_percentage_multi()}}.

Other import:
\code{\link[=parse_filename]{parse_filename()}},
\code{\link[=read_folder]{read_folder()}}
}
\concept{import}
