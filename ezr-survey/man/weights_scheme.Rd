% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{get_weights}
\alias{get_weights}
\alias{has_weights}
\alias{clear_weights}
\title{Inspect or clear the session weighting scheme}
\usage{
get_weights()

has_weights()

clear_weights()
}
\value{
\code{get_weights()} returns the scheme (a named list of target vectors;
error if none is set); \code{has_weights()} returns a logical; \code{clear_weights()}
returns \code{TRUE} invisibly.
}
\description{
Companions to \code{\link[=set_weights]{set_weights()}}: read the stored scheme, test whether one is set,
or remove it.
}
\details{
\code{has_weights()} is the safe check before \code{get_weights()}, which errors when no
scheme is set. \code{clear_weights()} turns weighting off again, so the helpers
return purely unweighted results.
}
\examples{
has_weights()
set_weights(c(variable = "region", Europe = 0.5, "North America" = 0.5))
get_weights()
clear_weights()
}
\seealso{
\code{\link[=set_weights]{set_weights()}}, \code{\link[=weight_vector]{weight_vector()}}.

Other weighting:
\code{\link[=clear_weights_cache]{clear_weights_cache()}},
\code{\link[=set_weights]{set_weights()}},
\code{\link[=weight_vector]{weight_vector()}}
}
\concept{weighting}
