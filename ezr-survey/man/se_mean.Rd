% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{se_mean}
\alias{se_mean}
\title{Standard error of a mean (point estimate)}
\usage{
se_mean(x)
}
\arguments{
\item{x}{A numeric vector. \code{NA} values are ignored.}
}
\value{
A single numeric standard error, or \code{NA} if fewer than two non-missing
values are present.
}
\description{
\code{sd(x) / sqrt(n)}, the sampling error of a mean such as an average 1-5
rating. Packages the inline \code{sem} calculation from the report appendix.
}
\details{
The standard error of the mean is the sample standard deviation divided by the
square root of the (non-missing) sample size, \code{sd(x) / sqrt(n)}. It shrinks as
the sample grows, and is the basis of the margin of error you quote for an
average rating. Fewer than two values gives \code{NA} (no spread to estimate).
}
\examples{
se_mean(c(4, 5, 3, 4, 5, 2, 4))
#> [1] 0.4285714
}
\seealso{
\code{\link[=se_prop]{se_prop()}}, \code{\link[=rse]{rse()}}, \code{\link[=margin_of_error]{margin_of_error()}}, \code{\link[=diagnose]{diagnose()}}.

Other diagnostics:
\code{\link[=diagnose]{diagnose()}},
\code{\link[=margin_of_error]{margin_of_error()}},
\code{\link[=precision_summary]{precision_summary()}},
\code{\link[=rse]{rse()}},
\code{\link[=rse_rating]{rse_rating()}},
\code{\link[=se_prop]{se_prop()}}
}
\concept{diagnostics}
