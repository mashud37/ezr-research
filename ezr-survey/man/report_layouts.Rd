% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_officer.R
\name{report_layouts}
\alias{report_layouts}
\title{List the slide layouts of a PowerPoint template}
\usage{
report_layouts(template = NULL, style = c("elevated", "plain"))
}
\arguments{
\item{template}{Path to a \code{.pptx}, an existing document from \code{\link[=report_new]{report_new()}},
or \code{NULL} (default) for the brand template registered by \code{\link[=use_brand]{use_brand()}}
(falling back to a built-in template, see \code{style}).}

\item{style}{Built-in template to inspect when no \code{template} and no brand
template are set: \code{"elevated"} (default) or \code{"plain"}. See \code{\link[=report_new]{report_new()}}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per layout: \code{layout},
\code{master}, \code{has_title}, \code{n_body}, \code{body_width}, \code{body_height} (inches of
the first body placeholder, \code{NA} when the layout has none).
}
\description{
Shows what a template offers the report builders: every layout, its master,
and the placeholders it carries. Useful to decide which \code{layout} to name in
\code{\link[=report_add_slide]{report_add_slide()}}, or to sanity-check an organisation template before
building a deck against it.
}
\details{
The report builders pick layouts automatically by inspecting placeholders,
so most decks never need this -- but when a corporate template offers
several content layouts, \code{report_layouts()} plus an explicit
\code{report_add_slide(layout = ...)} gives you full control. Requires the
suggested \code{officer} package.
}
\examples{
\dontshow{if (requireNamespace("officer", quietly = TRUE)) withAutoprint(\{ # examplesIf}
report_layouts()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=report_new]{report_new()}}, \code{\link[=report_add_slide]{report_add_slide()}}, \code{\link[=use_brand]{use_brand()}}.

Other reporting:
\code{\link[=example_report]{example_report()}},
\code{\link[=list_report_templates]{list_report_templates()}},
\code{\link[=report_add_plot]{report_add_plot()}},
\code{\link[=report_add_slide]{report_add_slide()}},
\code{\link[=report_add_table]{report_add_table()}},
\code{\link[=report_add_text]{report_add_text()}},
\code{\link[=report_deck]{report_deck()}},
\code{\link[=report_new]{report_new()}},
\code{\link[=report_save]{report_save()}},
\code{\link[=scaffold_report]{scaffold_report()}}
}
\concept{reporting}
