% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{precision_summary}
\alias{precision_summary}
\title{Plain-language survey precision summary}
\usage{
precision_summary(data = NULL, ..., z = 1.96)
}
\arguments{
\item{data}{A data frame. If omitted, the session default (\code{\link[=use_dataset]{use_dataset()}})
is used.}

\item{...}{Optional columns to restrict the assessment to (tidyselect). If
omitted, all suitable columns are used: every numeric column, and every
categorical column that is not free text or an identifier.}

\item{z}{Confidence multiplier for the margins of error. Default \code{1.96}
(95\%).}
}
\value{
An object of class \code{ezrsurvey_precision} (a list with \code{n}, the
per-variable \code{table}, \code{overall_rse}, \code{rating} and the \code{bullets}). Printing it
shows the bullet points.
}
\description{
Distils a whole survey's sampling precision into a few bullet points -- the
narrative "Data info" paragraph from the report appendix, generated
automatically. It scans the dataset, assesses the numeric (point-scale) and
categorical (proportion) variables, and reports the typical sampling margins
and an overall reliability verdict.
}
\details{
Rather than make you choose which variables to quote precision for, this picks
them: numeric columns become point estimates (margins in rating points) and
categorical columns become proportions (margins in percentage points), with
free-text and id-like columns skipped. It then reports the \strong{typical} and
\strong{worst} margins across each group, plus an overall verdict from the median
relative standard error via \code{\link[=rse_rating]{rse_rating()}}. For proportions it also gives the
worst-case \verb{+/-} percentage-point margin at a 50/50 split, which is the widest
any percentage in the study can be. See \code{vignette("ezrsurvey")} for the
rationale behind the relative-standard-error reliability bands.
}
\examples{
precision_summary(consumer_survey)
}
\seealso{
\code{\link[=diagnose]{diagnose()}}, \code{\link[=rse_rating]{rse_rating()}}.

Other diagnostics:
\code{\link[=diagnose]{diagnose()}},
\code{\link[=margin_of_error]{margin_of_error()}},
\code{\link[=rse]{rse()}},
\code{\link[=rse_rating]{rse_rating()}},
\code{\link[=se_mean]{se_mean()}},
\code{\link[=se_prop]{se_prop()}}
}
\concept{diagnostics}
