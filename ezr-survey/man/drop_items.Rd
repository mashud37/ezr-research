% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recode.R
\name{drop_items}
\alias{drop_items}
\title{Drop unwanted answer categories from a question}
\usage{
drop_items(x, items, trim = TRUE)
}
\arguments{
\item{x}{A character (or factor) vector.}

\item{items}{Character vector of exact answer values to drop. Matching is
case-insensitive.}

\item{trim}{Whether to trim surrounding whitespace before comparing. Defaults
to \code{TRUE}.}
}
\value{
A character vector the same length as \code{x}, with any value matching
\code{items} replaced by \code{NA}.
}
\description{
Removes specific answer values -- typically catch-all or uninformative options
such as "Other", "Don't know" or "Not applicable" -- by turning them into
\code{NA}, so they fall out of counts, percentages and charts and the remaining
percentages re-base on the answers you keep. The companion to \code{\link[=na_blank]{na_blank()}}
(which targets blanks and standard non-answers): use \code{drop_items()} for the
substantive categories you simply do not want to show.
}
\details{
\code{x} is coerced to character (so factors are handled) and, by default,
whitespace-trimmed; any value equal (ignoring case) to one of \code{items} becomes
\code{NA}. The summary helpers (\code{\link[=calc_percentage]{calc_percentage()}}, \code{\link[=calc_percentage_multi]{calc_percentage_multi()}},
\code{\link[=calc_percentage_batch]{calc_percentage_batch()}}) and \code{\link[=crosstab]{crosstab()}} take a \verb{drop =} argument that
applies this for you \emph{before} counting, so the kept answers re-base to ~100\%;
set a session-wide default with \code{ezrsurvey_options(drop_answers = ...)}.
}
\examples{
drop_items(c("Yes", "No", "Other", "Don't know"),
           items = c("Other", "Don't know"))
#> [1] "Yes" "No"  NA    NA
}
\seealso{
\code{\link[=na_blank]{na_blank()}}, \code{\link[=calc_percentage]{calc_percentage()}}.

Other recode:
\code{\link[=add_region]{add_region()}},
\code{\link[=bin_numeric]{bin_numeric()}},
\code{\link[=ensure_numeric]{ensure_numeric()}},
\code{\link[=generation_scheme]{generation_scheme()}},
\code{\link[=na_blank]{na_blank()}},
\code{\link[=nps_group]{nps_group()}},
\code{\link[=recode_age]{recode_age()}},
\code{\link[=recode_generation]{recode_generation()}},
\code{\link[=recode_likert]{recode_likert()}},
\code{\link[=recode_region]{recode_region()}}
}
\concept{recode}
