% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percentage.R
\name{calc_percentage_multi}
\alias{calc_percentage_multi}
\title{Tabulate a check-all-that-apply (multi-select) question as percentages}
\usage{
calc_percentage_multi(
  data = NULL,
  prefix,
  id = NULL,
  by = NULL,
  sort = c("none", "desc", "asc"),
  digits = 0,
  drop = NULL,
  clean_names = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{prefix}{Common column-name prefix identifying the option block, e.g.
\code{"motivations_"}.}

\item{id}{Optional respondent identifier column (unquoted) used as the
distinct-respondent denominator. If omitted, each row is treated as one
respondent.}

\item{by}{Optional grouping column(s); percentages are computed within group.}

\item{sort, digits}{As in \code{\link[=calc_percentage]{calc_percentage()}}. Sorting acts on the \code{option}
labels.}

\item{drop}{Optional character vector of option labels to remove before
counting (matched against the cleaned labels, case-insensitive). Defaults to
the \code{drop_answers} option. See \code{\link[=drop_items]{drop_items()}}.}

\item{clean_names}{If \code{TRUE} (default), tidy the option labels by stripping
\code{prefix} and un-mangling exporter artefacts (\code{"A...B"} -> \code{"A / B"}).}
}
\value{
A \link[tibble:tibble]{tibble} with \code{option}, \code{n} (respondents choosing
it) and \code{pct}.
}
\description{
Multi-select questions arrive as a block of columns sharing a prefix, each
holding the chosen option (or blank). This computes, per option, the share of
respondents who selected it -- so the percentages can (and usually do) sum to
more than 100. The denominator is the number of distinct respondents who
selected at least one option, reproducing the \code{freq_multiple()} helper from
the original reports.
}
\details{
The crucial difference from \code{\link[=calc_percentage]{calc_percentage()}} is the denominator: it is the
number of distinct respondents who picked \emph{any} option in the block, not the
number of ticks. So if 700 of 1,000 respondents selected at least one
motivation, each option's percentage is "out of 700", and the percentages can
(and usually do) sum to more than 100. Supply \code{id} to count distinct
respondents exactly; without it, each row counts as one respondent. With
\code{clean_names = TRUE} the option labels are stripped of \code{prefix} and exporter
artefacts like \code{"A...B"} are turned back into \code{"A / B"}.
}
\examples{
calc_percentage_multi(podracing_survey, "motivations_",
                      id = respondent_id, sort = "desc")
#> # A tibble: 5 x 3
#>   option        n   pct
#>   <fct>     <int> <dbl>
#> 1 speed       772    79
#> 2 drivers     533    54
#> 3 social      437    45
#> 4 tradition   349    36
#> 5 betting     295    30
}
\seealso{
\code{\link[=calc_percentage]{calc_percentage()}}, \code{\link[=plot_bars]{plot_bars()}}.

Other summaries:
\code{\link[=calc_percentage]{calc_percentage()}},
\code{\link[=calc_percentage_batch]{calc_percentage_batch()}},
\code{\link[=calc_summary]{calc_summary()}},
\code{\link[=crosstab]{crosstab()}}
}
\concept{summaries}
