% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare_values}
\alias{compare_values}
\title{Compare a metric between two datasets (e.g. two events or waves)}
\usage{
compare_values(current, previous, by = "feature", value = "performance")
}
\arguments{
\item{current, previous}{Data frames sharing a key column and a metric column.}

\item{by}{Name of the key column to join on. Default \code{"feature"}.}

\item{value}{Name of the metric column to compare. Default \code{"performance"}.}
}
\value{
A \link[tibble:tibble]{tibble} with the key column, \code{previous}, \code{current}
and \code{difference} (\code{current - previous}).
}
\description{
Joins a \code{current} and a \code{previous} table on a key column and computes the
difference of a metric -- for example feature \code{performance} from two
\code{\link[=ipm_model]{ipm_model()}} outputs, or \code{pct} from two \code{\link[=calc_percentage]{calc_percentage()}} tables for two
events. The result feeds straight into \code{\link[=plot_diff]{plot_diff()}}.
}
\details{
The join is a left join keyed on \code{by} with \code{previous} on the left, so the row
set follows the previous period and \code{difference} is \code{current - previous} (a
positive number means it went up). Keys present only in \code{current} are dropped;
keys missing from \code{current} get an \code{NA} difference. The inputs are deliberately
generic two-column-or-more tables, so the same function compares feature
\code{performance} from two \code{\link[=ipm_model]{ipm_model()}} runs, \code{pct} from two \code{\link[=calc_percentage]{calc_percentage()}}
tables for two events, or any other keyed metric.
}
\examples{
a <- data.frame(feature = c("price", "quality"), performance = c(3.1, 4.2))
b <- data.frame(feature = c("price", "quality"), performance = c(2.8, 4.4))
compare_values(current = b, previous = a)
#> # A tibble: 2 x 4
#>   feature previous current difference
#>   <chr>      <dbl>   <dbl>      <dbl>
#> 1 price        3.1     2.8      -0.3
#> 2 quality      4.2     4.4       0.2
}
\seealso{
\code{\link[=plot_diff]{plot_diff()}}, \code{\link[=ipm_model]{ipm_model()}}, \code{\link[=calc_percentage]{calc_percentage()}}.

Other compare:
\code{\link[=plot_diff]{plot_diff()}}
}
\concept{compare}
