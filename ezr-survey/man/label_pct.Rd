% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{label_pct}
\alias{label_pct}
\title{Percent label formatter}
\usage{
label_pct(digits = 0, suffix = "\%")
}
\arguments{
\item{digits}{Number of decimal places to keep. Defaults to \code{0}.}

\item{suffix}{String appended after the number. Defaults to \code{"\%"}.}
}
\value{
A function suitable for the \code{labels} argument of a ggplot2 scale or
for use with \link[ggplot2:geom_text]{geom_text()}.
}
\description{
Returns a function that turns numbers into percent strings, e.g. \code{42}
becomes \code{"42\%"}. The input is assumed to already be on a 0-100 scale (as
produced by \code{\link[=calc_percentage]{calc_percentage()}}), not a 0-1 proportion.
}
\details{
\code{label_pct()} returns a \emph{function} (a closure), not formatted strings -- this
is the form ggplot2 scales expect for their \code{labels} argument. The number is
rounded to \code{digits} decimal places with \code{\link[=formatC]{formatC()}} and \code{suffix} appended.
Because the input is assumed to be on a 0--100 scale, \code{label_pct()(42)} is
\code{"42\%"}, not \code{"4200\%"}.
}
\examples{
f <- label_pct()
f(c(0, 33.4, 100))
#> [1] "0\%"   "33\%"  "100\%"

label_pct(1)(33.45)
#> [1] "33.5\%"
}
\seealso{
\code{\link[=scale_y_pct]{scale_y_pct()}}.

Other scales:
\code{\link[=nice_max]{nice_max()}},
\code{\link[=scale_y_pct]{scale_y_pct()}}
}
\concept{scales}
