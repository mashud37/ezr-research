% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{parse_filename}
\alias{parse_filename}
\title{Split a filename column into metadata columns}
\usage{
parse_filename(
  data = NULL,
  col = "file",
  into,
  sep = "_",
  drop_ext = TRUE,
  remove = FALSE
)
}
\arguments{
\item{data}{A data frame.}

\item{col}{Name of the column holding the filename (string or unquoted).
Defaults to \code{"file"}.}

\item{into}{Character vector of new column names, one per field you expect
after splitting.}

\item{sep}{Separator to split on (a fixed string, not a regex). Defaults to
\code{"_"}.}

\item{drop_ext}{If \code{TRUE} (default), strip a trailing file extension before
splitting.}

\item{remove}{If \code{TRUE}, drop the original filename column. Defaults to
\code{FALSE} so you keep the source reference.}
}
\value{
The data frame with the new metadata columns added.
}
\description{
Survey exports often encode metadata in the filename (e.g.
\code{"viewer_CDL_brand_NA_2026.csv"}). This splits a filename column on a
separator into named metadata columns, dropping the file extension first.
Replaces the brittle \code{str_split(file, "_")[[1]][n]} indexing in the original
scripts.
}
\details{
Pairs naturally with \code{\link[=read_folder]{read_folder()}}, whose \code{file} column carries the source
filename: split it once and you have event, wave, locale, year, etc. as real
columns to group by. The file extension is stripped first (\code{drop_ext}), the
split is on a fixed string (not a regex), and rows with fewer fields than
\code{into} are padded with \code{NA} so a stray filename never derails the parse.
}
\examples{
df <- tibble::tibble(file = c("viewer_CDL_acme_NA_2026.csv"))
parse_filename(df, into = c("type", "game", "brand", "locale", "year"))
}
\seealso{
\code{\link[=read_folder]{read_folder()}}.

Other import:
\code{\link[=read_folder]{read_folder()}},
\code{\link[=select_prefix]{select_prefix()}}
}
\concept{import}
