% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_stacked_rating}
\alias{plot_stacked_rating}
\title{Stacked rating bars with weighted-average ordering}
\usage{
plot_stacked_rating(
  data,
  feature,
  level,
  value = pct,
  palette = NULL,
  label_min = 1,
  show_average = TRUE
)
}
\arguments{
\item{data}{Long data with one row per feature x rating level.}

\item{feature}{Feature column (unquoted).}

\item{level}{Ordered rating-level column (unquoted), e.g. a factor like
\code{"1 - Very bad"} .. \code{"5 - Very good"}. The leading digit is used as the
numeric weight.}

\item{value}{Percentage column (unquoted). Defaults to \code{pct}.}

\item{palette}{Fill colours, named by level or positional. Defaults to
\link{pal_rating} (use \code{scale_fill_rating(distinct = TRUE)} colours via
\code{palette = NULL} to get the 5-distinct ramp).}

\item{label_min}{Hide segment labels below this percentage. Defaults to \code{1}.}

\item{show_average}{Append the weighted mean to each feature label. Defaults
to \code{TRUE}.}
}
\value{
A ggplot object.
}
\description{
Draws a 100\%-stacked bar per feature across an ordinal rating scale, labels
each segment, and orders features by their weighted mean rating -- the
likeability / purchase / feature-rating charts from the original reports.
}
\details{
Each feature becomes a 100\%-stacked bar across the rating scale, and features
are ordered by their weighted-mean rating (the leading digit of each \code{level}
is the weight), so the best-rated feature sits at the top. The weighted mean
is appended to each feature label when \code{show_average = TRUE}. Feed it a long
table with one row per feature x rating level; the \verb{partner_*} likeability and
purchase questions in the source reports are the canonical use.
}
\examples{
rating_long <- consumer_survey |>
  dplyr::select(dplyr::starts_with("ratings_")) |>
  tidyr::pivot_longer(dplyr::everything(),
                      names_to = "feature", values_to = "level") |>
  dplyr::filter(level != "") |>
  dplyr::mutate(level = paste0(recode_likert(level), " - ", level)) |>
  dplyr::count(feature, level) |>
  dplyr::group_by(feature) |>
  dplyr::mutate(pct = n / sum(n) * 100) |>
  dplyr::ungroup()
p <- plot_stacked_rating(rating_long, feature, level)
}
\seealso{
\code{\link[=calc_percentage]{calc_percentage()}}, \code{\link[=scale_fill_rating]{scale_fill_rating()}}.

Other plots:
\code{\link[=plot_bars]{plot_bars()}},
\code{\link[=plot_ipm]{plot_ipm()}},
\code{\link[=plot_nps]{plot_nps()}},
\code{\link[=plot_nps_gauge]{plot_nps_gauge()}},
\code{\link[=plot_quotes_tree]{plot_quotes_tree()}}
}
\concept{plots}
