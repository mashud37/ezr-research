% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percentage.R
\name{clean_label}
\alias{clean_label}
\title{Tidy an exported column name into a readable label}
\usage{
clean_label(x, prefix = NULL)
}
\arguments{
\item{x}{A character vector of column names.}

\item{prefix}{Optional prefix to strip from the start of each name before
tidying, e.g. \code{"ratings_"}. Names that do not start with it are left alone.}
}
\value{
A character vector of labels, the same length as \code{x}.
}
\description{
Turns the mangled column names survey tools produce back into the wording a
reader expects: \code{"Broadcast.quality...audio"} becomes
\code{"Broadcast quality / audio"}. Drop a question prefix at the same time with
\code{prefix}, so a block of \verb{ratings_*} columns becomes plain feature names.
}
\details{
Exporters replace every character they cannot put in a column name with a
dot, so a slash surrounded by spaces arrives as three dots and a space
arrives as one. This restores both, in that order, then squishes the result.
It is the same cleaning \code{\link[=calc_percentage_multi]{calc_percentage_multi()}} and \code{\link[=ipm_model]{ipm_model()}} apply to
their own output, exported so a hand-built table can match them: labels that
disagree will not join, which is what silently empties a comparison chart.
}
\examples{
clean_label("Broadcast.quality...audio")
#> [1] "Broadcast quality / audio"

clean_label(c("ratings_Camera.work", "ratings_Talent...analysis"),
            prefix = "ratings_")
#> [1] "Camera work"       "Talent / analysis"
}
\seealso{
\code{\link[=calc_percentage_batch]{calc_percentage_batch()}}, \code{\link[=calc_percentage_multi]{calc_percentage_multi()}}.

Other recode:
\code{\link[=add_region]{add_region()}},
\code{\link[=bin_numeric]{bin_numeric()}},
\code{\link[=drop_items]{drop_items()}},
\code{\link[=ensure_numeric]{ensure_numeric()}},
\code{\link[=generation_scheme]{generation_scheme()}},
\code{\link[=na_blank]{na_blank()}},
\code{\link[=nps_group]{nps_group()}},
\code{\link[=recode_age]{recode_age()}},
\code{\link[=recode_generation]{recode_generation()}},
\code{\link[=recode_likert]{recode_likert()}},
\code{\link[=recode_region]{recode_region()}}
}
\concept{recode}
