% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comments.R
\name{sample_comments_diverse}
\alias{sample_comments_diverse}
\title{Pick a diverse, information-rich sample of comments}
\usage{
sample_comments_diverse(
  data = NULL,
  ...,
  n = 8,
  min_chars = 30,
  max_chars = 400,
  exclude = NULL,
  lambda = 0.6,
  method = c("mmr", "entropy"),
  score = c("entropy", "tfidf"),
  stopwords = NULL,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{...}{One or more comment columns, using tidyselect (e.g. \code{nps_com},
\code{show_com}, or \code{ends_with("_com")}).}

\item{n}{Number of comments to sample. With \code{by_column = TRUE}, this is per
column.}

\item{min_chars}{Drop comments with this many characters or fewer. Default
\code{30}.}

\item{max_chars}{Truncate longer comments to this length. Default \code{400}.}

\item{exclude}{Optional character vector of (case-insensitive) regex terms;
comments matching any are dropped (e.g. \code{c("friend", "recommend")}).}

\item{lambda}{Diversity/informativeness trade-off in \verb{[0, 1]} for
\code{method = "mmr"}: higher favours informative comments, lower favours
dissimilar ones. Default \code{0.6}.}

\item{method}{\code{"mmr"} (default) for the diversity-aware greedy selection, or
\code{"entropy"} to weight a random sample by each comment's information score.}

\item{score}{By which to measure information: \code{"entropy"} (default, Shannon
bits) or \code{"tfidf"} (summed TF-IDF weight).}

\item{stopwords}{Stop-word handling: \code{NULL} (default) uses the \code{stopwords}
package when installed (English), otherwise a small built-in fallback;
a character vector supplies your own list; \code{FALSE} disables removal. Pass
e.g. \code{stopwords::stopwords("de")} for another language.}

\item{seed}{Optional integer for reproducible sampling (the RNG state is
restored afterwards).}
}
\value{
A \link[tibble:tibble]{tibble} with \code{source}, \code{comment}, \code{length} and the
\code{info} score, ordered by selection.
}
\description{
A smarter alternative to \code{\link[=sample_comments]{sample_comments()}}: instead of sampling uniformly,
it scores each comment and selects a set that is both \strong{informative} and
\strong{varied}, so a quote slide isn't five ways of saying the same thing. Scoring
is lexical -- comments are turned into TF-IDF vectors, an information score is
computed (Shannon entropy of the word distribution, or TF-IDF mass), and a
randomised maximal-marginal-relevance pass trades off informativeness against
similarity to already-picked comments.
}
\examples{
sample_comments_diverse(podracing_survey, nps_com, show_com, n = 5, seed = 1)
}
\seealso{
\code{\link[=sample_comments]{sample_comments()}}, \code{\link[=plot_quotes_tree]{plot_quotes_tree()}}.

Other comments:
\code{\link[=sample_comments]{sample_comments()}}
}
\concept{comments}
